# Implementation Plan: Add optional code review hook to solve-adr branch completion

**Source ADRs:**
- [ADR-0059: Add optional code review hook to solve-adr branch completion](docs/adr/0059-add-optional-code-review-hook-to-solve-adr-branch-completion.md)

**Generated:** 2026-04-07
**Location:** `docs/plans/0059.0.plan.md`
**Revision:** 0

---

## Task Execution Protocol

When executing any task from this plan, follow this protocol:

1. **Before starting a task** — read the task's **Test & Acceptance Criteria** section in full.
2. **As each criterion is satisfied** — immediately edit this plan file to mark the checkbox from `- [ ]` to `- [x]`.
3. **After completing all implementation work** for a task — review the criteria list one final time. Any criterion still unchecked is either incomplete work (go back and finish it) or an oversight (check it off with a brief inline note if it was already satisfied by prior work).
4. **Do not batch checkbox updates** to the end of a stage — update them incrementally so that progress is visible in the file at all times.

---

## Stage 1: Configuration and Startup

### Task 1.1: Add `[solve.dispatch]` configuration to SKILL.md    [small]

**Description:** Add the `[solve.dispatch]` configuration section to solve-adr's SKILL.md Configuration area. Document the `code_review` key, its expected values (agent reference or absent to skip), and the fallback behavior.

**Implementation Notes:**

Add under the existing Configuration section, after the project-scoped block:

```toml
[solve.dispatch]
code_review = ""  # agent reference (e.g., "juliusl-code-reviewer-v1"), or absent/empty to skip
```

Follow the same documentation pattern as `[author.dispatch]` in author-adr's SKILL.md.

**Test & Acceptance Criteria:**
- [x] `[solve.dispatch]` section is documented in SKILL.md's Configuration area
- [x] The `code_review` key description explains: agent reference to dispatch, absent/empty means skip
- [x] Fallback behavior documented: if agent cannot be resolved, warn and skip Step 4d
- [x] No unrelated changes to Configuration section

**Dependencies:** None

**ADR Reference:** ADR-0059, Decision §Configuration

---

### Task 1.2: Update S-0 Startup to load `[solve.dispatch]` keys    [small]

**Description:** Update the S-0 Startup procedure in SKILL.md to load `[solve.dispatch]` keys alongside the existing `[author.dispatch]` loading in step 4.

**Implementation Notes:**

In S-0 step 4 ("Load dispatch config"), extend the instruction to also read `[solve.dispatch]` keys:
- Read `[solve.dispatch].code_review` if present
- Store for use during Step 4d

**Test & Acceptance Criteria:**
- [x] S-0 step 4 reads `[solve.dispatch].code_review` alongside `[author.dispatch]` keys
- [x] Absent key results in Step 4d being skipped (documented behavior)
- [x] No changes to S-0 steps 1–3 or step 5

**Dependencies:** Task 1.1

**ADR Reference:** ADR-0059, Decision §Step 4d mechanics, point 1

---

## Stage 2: Lifecycle and Step 4d

### Task 2.1: Update lifecycle diagrams in SKILL.md    [small]

**Description:** Update the S-1 Problem lifecycle flow in SKILL.md to include Step 4d between Step 4 (Implement) and Step 5 (Report). Update the procedure table.

**Implementation Notes:**

Update the lifecycle diagram:
```
4. Implement — group accepted ADRs, load /implement-adr and run its procedure
   ↓
4d. Code Review — dispatch configured agent to review branch diff (optional)
   ↓
5. Report — summarize what was implemented, what remains
```

**Test & Acceptance Criteria:**
- [x] S-1 lifecycle diagram includes Step 4d between Steps 4 and 5
- [x] Step 4d is marked as optional in the diagram
- [x] "Branch name storage" paragraph updated to document base branch storage alongside branch name
- [x] SKILL.md line count remains under 500

**Dependencies:** Stage 1

**ADR Reference:** ADR-0059, Decision §Step 4d mechanics, §Base branch tracking

---

### Task 2.2: Add Step 4d detail to references/problem.md    [medium]

**Description:** Add the full Step 4d (Code Review) procedure to problem.md. This includes the procedure flow update, procedure table update, and a new Step 4d section with the full mechanics: base branch detection, agent dispatch, finding collection, triage, and gate.

**Implementation Notes:**

Add a new section after Step 4 (Implement) and before Step 5 (Report):

```markdown
## Step 4d: Code Review (Optional)

After all implementation groups complete (Step 4c), optionally dispatch a configured code review agent to review the cumulative diff of the solve branch.

**Entry condition:** `[solve.dispatch].code_review` is configured and non-empty. If absent or empty, skip this step and proceed to Step 5.
```

Include the 5-point mechanics from ADR-0059 Decision section:
1. Determine base branch
2. Dispatch code review agent
3. Collect findings
4. Triage findings (autonomous vs. guided mode)
5. Gate (high-priority findings block Step 5)

**Test & Acceptance Criteria:**
- [ ] Step 4d section added to problem.md between Step 4 and Step 5
- [ ] Procedure flow diagram updated to include Step 4d
- [ ] Procedure table updated with Step 4d and its sub-steps
- [ ] Entry condition documented: absent/empty `code_review` key → skip
- [ ] All 5 mechanics points from ADR-0059 are covered
- [ ] Autonomous vs. guided triage behavior specified
- [ ] Gate behavior specified: unaddressed high-priority findings block Step 5
- [ ] Resume protocol updated: add state for "all ADRs Accepted, Step 4d not yet run → enter Step 4d"
- [ ] Step 5 updated to reference Step 4d completion as prerequisite

**Dependencies:** Task 2.1

**ADR Reference:** ADR-0059, Decision §Step 4d mechanics

---

### Task 2.3: Update Step 1b to record base branch    [small]

**Description:** Update Step 1b (Create Feature Branch) in problem.md to record the base branch in session state alongside the branch name. This is needed by Step 4d to compute the cumulative diff.

**Implementation Notes:**

In Step 1b step 4 ("Create and checkout"), add an instruction to record the current branch as the base branch before creating the solve branch. Example: "Record the current branch (`git rev-parse --abbrev-ref HEAD`) as the base branch in session state."

**Test & Acceptance Criteria:**
- [ ] Step 1b records the base branch in session state
- [ ] The instruction is clear about when to capture (before `git checkout -b`)
- [ ] Step 4d references this stored base branch

**Dependencies:** Task 2.2

**ADR Reference:** ADR-0059, Decision §Base branch tracking

---

## Stage 3: Finalize

### Task 3.1: Update ADR status    [small]

**Description:** Update the status of ADR-0059 from `Planned` to `Accepted` to reflect that the decision has been fully implemented.

> **Note:** ADR-0059 will be in `Planned` status at this point — it was transitioned from `Ready` to `Planned` in Step I-5 before execution began.

**Files to update:**
- `docs/adr/0059-add-optional-code-review-hook-to-solve-adr-branch-completion.md` — change `## Status` from `Planned` to `Accepted`

**Test & Acceptance Criteria:**
- [ ] ADR-0059 status reads `Accepted`
- [ ] No other ADR content is modified

**Dependencies:** All prior stages

**ADR Reference:** ADR-0003, Decision §2

---

## Summary

| Stage | Task | Cost | Dependencies |
|-------|------|------|--------------|
| 1. Configuration and Startup | 1.1 Add `[solve.dispatch]` config to SKILL.md | small | — |
| 1. Configuration and Startup | 1.2 Update S-0 to load `[solve.dispatch]` keys | small | 1.1 |
| 2. Lifecycle and Step 4d | 2.1 Update lifecycle diagrams in SKILL.md | small | Stage 1 |
| 2. Lifecycle and Step 4d | 2.2 Add Step 4d detail to problem.md | medium | 2.1 |
| 2. Lifecycle and Step 4d | 2.3 Update Step 1b base branch recording | small | 2.2 |
| 3. Finalize | 3.1 Update ADR status | small | All prior |

**Total estimated cost:** 5 small, 1 medium, 0 heavy
