# Implementation Plan: Incremental Progress Tracking During Plan Execution

**Source ADRs:**
- [ADR-0009: Implement-adr uses incremental progress tracking during plan execution](docs/adr/0009-implement-adr-uses-incremental-progress-tracking-during-plan-execution.md)

**Generated:** 2026-04-02
**Location:** `docs/plans/0009.0.plan.md`
**Revision:** 0

---

## Task Execution Protocol

When executing any task from this plan, follow this protocol:

1. **Before starting a task** — read the task's **Test & Acceptance Criteria**
   section in full.
2. **As each criterion is satisfied** — immediately edit this plan file to mark
   the checkbox from `- [ ]` to `- [x]`.
3. **After completing all implementation work** for a task — review the criteria
   list one final time. Any criterion still unchecked is either incomplete work
   (go back and finish it) or an oversight (check it off with a brief inline
   note if it was already satisfied by prior work).
4. **Do not batch checkbox updates** to the end of a stage — update them
   incrementally so that progress is visible in the file at all times.

---

## Stage 1: Embed Protocol in Plan Template

### Task 1.1: Add Task Execution Protocol section to plan-template.md    [small]

**Description:** Add a "Task Execution Protocol" section to the plan template
at `src/skills/implement-adr/assets/templates/plan-template.md`. The section
goes between the header (revision metadata) and the first stage. It must
contain the four protocol steps from ADR-0009, Decision §1 and be fenced by
horizontal rules (`---`) above and below.

**Implementation Notes:**

Insert after the revision comment block and before `## Stage 1`:

```markdown
---

## Task Execution Protocol

When executing any task from this plan, follow this protocol:

1. **Before starting a task** — read the task's **Test & Acceptance Criteria**
   section in full.
2. **As each criterion is satisfied** — immediately edit this plan file to mark
   the checkbox from `- [ ]` to `- [x]`.
3. **After completing all implementation work** for a task — review the criteria
   list one final time. Any criterion still unchecked is either incomplete work
   (go back and finish it) or an oversight (check it off with a brief inline
   note if it was already satisfied by prior work).
4. **Do not batch checkbox updates** to the end of a stage — update them
   incrementally so that progress is visible in the file at all times.

---
```

**Test & Acceptance Criteria:**
- [x] `src/skills/implement-adr/assets/templates/plan-template.md` contains a `## Task Execution Protocol` section
- [x] The section appears between the header metadata and `## Stage 1`
- [x] All four protocol steps from ADR-0009 Decision §1 are present verbatim
- [x] The section is fenced by `---` above and below

**Dependencies:** None

**ADR Reference:** ADR-0009, Decision §1

---

### Task 1.2: Verify plan template renders correctly    [small]

**Description:** Confirm the updated template is well-formed markdown and that
the protocol section does not interfere with existing template structure
(stages, tasks, summary table, decision gaps).

**Test & Acceptance Criteria:**
- [x] Template line count is reasonable (under 150 lines) — 133 lines
- [x] All existing template sections remain intact (Stage 1, Stage 2, Stage N: Finalize, Summary, Decision Gaps)
- [x] `make test` passes (no regressions in adr-tools or madr-tools)

**Dependencies:** Task 1.1

**ADR Reference:** ADR-0009, Decision §1, §3

---

## Stage 2: Finalize

### Task 2.1: Confirm ADR-0009 status is Accepted    [small]

**Description:** Verify that ADR-0009's status reads `Accepted` to reflect
that the decision has been fully implemented.

**Files to verify:**
- `docs/adr/0009-implement-adr-uses-incremental-progress-tracking-during-plan-execution.md` — status should read `Accepted`

**Test & Acceptance Criteria:**
- [x] ADR-0009 status reads `Accepted`
- [x] No other ADR content is modified

**Dependencies:** All prior stages

**ADR Reference:** ADR-0003, Decision §2

---

## Summary

| Stage | Task | Cost | Dependencies |
|-------|------|------|--------------|
| 1. Embed Protocol | 1.1 Add Task Execution Protocol section to plan-template.md | small | — |
| 1. Embed Protocol | 1.2 Verify plan template renders correctly | small | 1.1 |
| 2. Finalize | 2.1 Confirm ADR-0009 status is Accepted | small | All prior |

**Total estimated cost:** 3 small, 0 medium, 0 heavy
