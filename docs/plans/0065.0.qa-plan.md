# QA Plan: Refine dispatch hook behaviors based on dogfood evidence

**Source Plan:** docs/plans/0065.0.plan.md
**Generated:** 2026-04-10
**Location:** `docs/plans/0065.0.qa-plan.md`

---

## Stage 1: Create.md Refinements

### Security Checks

- [x] **S1.1** — No user-supplied strings interpolated into SQL, shell commands, or file paths — N/A: instruction text only, no executable code
- [x] **S1.2** — No secrets, credentials, or API keys in committed files — ✅ Verified: diff contains only instruction prose, no sensitive data
- [x] **S1.3** — No deserialization of untrusted input without validation — N/A: no deserialization
- [x] **S1.4** — Dependencies pinned to specific versions (no wildcards) — N/A: no dependency changes
- [x] **S1.5** — File permissions on created artifacts are not overly permissive — N/A: modifying existing file only
- [x] **S1.6** — Any new external input surface has validation at the boundary — N/A: no new input surfaces

### UX Checks

- [x] **U1.1** — Every error path produces a human-readable message on stderr — N/A: instruction text, not executable code
- [x] **U1.2** — Every user-facing command exits with code 0 on success, non-zero on failure — N/A: no commands
- [x] **U1.3** — Invalid input is rejected with a helpful message, not a crash — N/A: no input validation code
- [x] **U1.4** — Resources (file handles, database connections) are cleaned up on error paths — N/A: no resources
- [x] **U1.5** — If the stage writes data, there is a way to read it back or verify it — ✅ Verdict routing in create.md uses bullet-point format with bold tier names; scannable and unambiguous
- [x] **U1.6** — If the stage creates state, there is a way to inspect the new state — ✅ All four tiers visible: Step 4a (lines 240-243: Redesign, Revise — Major, Revise, Accept); Step 4b (lines 264-267: not-ready fundamental, not-ready addressable→Revise — Major, ready with findings, ready)
- [x] **U1.7** — A user who did not write the code can verify the stage's output — ✅ Each tier maps to exactly one action with no overlap or ambiguity

### Test-Gap Findings

- **TG1.1** — **Consistent "Revise — Major" formatting across steps.** ✅ PASSED. Both occurrences use identical byte sequences: `Revise` + U+2014 em dash (e2 80 94) + `Major`. Verified via xxd on lines 241 and 265.

- **TG1.2** — **Stale binary examples in Step 4a prose.** ✅ PASSED. Step 4a step 3 routing bullets were updated: Redesign retains its bullet with added clarification ("Redesign means the approach needs rethinking"), Revise — Major was added as a new bullet, Revise retains its bullet. No stale binary-only references remain.

- **TG1.3** — **Step 4b "not-ready" split lacks an observable distinguishing rule.** ✅ PASSED. Line 270 contains an explicit distinguishing rule: "Fundamental gaps involve missing START criteria, detected decision-making fallacies, or identified anti-patterns — these indicate the decision framework is unsound. Addressable gaps involve missing detail within an otherwise sound framework — incomplete analysis, missing drivers, or unclear scope that can be fixed by revising the existing content."

---

## Stage 2: Polish.md Refinements and Consistency

### Security Checks

- [x] **S2.1** — No user-supplied strings interpolated into SQL, shell commands, or file paths — N/A: instruction text only
- [x] **S2.2** — No secrets, credentials, or API keys in committed files — ✅ Verified: diff contains only instruction prose, no sensitive data
- [x] **S2.3** — No deserialization of untrusted input without validation — N/A: no deserialization
- [x] **S2.4** — Dependencies pinned to specific versions (no wildcards) — N/A: no dependency changes
- [x] **S2.5** — File permissions on created artifacts are not overly permissive — N/A: modifying existing files only
- [x] **S2.6** — Any new external input surface has validation at the boundary — N/A: no new input surfaces

### UX Checks

- [x] **U2.1** — Every error path produces a human-readable message on stderr — N/A: instruction text, not executable code
- [x] **U2.2** — Every user-facing command exits with code 0 on success, non-zero on failure — N/A: no commands
- [x] **U2.3** — Invalid input is rejected with a helpful message, not a crash — N/A: no input validation code
- [x] **U2.4** — Resources (file handles, database connections) are cleaned up on error paths — N/A: no resources
- [x] **U2.5** — If the stage writes data, there is a way to read it back or verify it — ✅ R-6a threshold in polish.md (lines 227-230) uses clear conditional structure: ≥3 OR Medium+ → dispatch; ≤2 Low → inline. Decision is unambiguous.
- [x] **U2.6** — If the stage creates state, there is a way to inspect the new state — ✅ SKILL.md A-3 step 7 (line 219) updated with threshold language. Dispatch hook table (lines 136-143) remains accurate for all six hooks.
- [x] **U2.7** — A user who did not write the code can verify the stage's output — ✅ Cross-file search for "Revise — Major" returns 2 hits in create.md (Step 4a, Step 4b), 0 in polish.md/SKILL.md (correct — those files don't use verdict tiers). R-6a threshold appears in polish.md and is mirrored in SKILL.md A-3 step 7.

### Test-Gap Findings

- **TG2.1** — **Severity classification undefined in R-6a context.** ⚠️ Deferred. R-6a references "Low-severity" and "Medium+ severity" but does not explicitly map these terms to the existing (H)/(M)/(L) priority levels defined in the Revision Phase (V-1, lines 276-279). The mapping is natural ((L)→Low, (M)/(H)→Medium+) and any review agent would classify its own suggestions, but the connection is implicit rather than explicit. This does not block implementation — the threshold is implementable — but a future pass could add an explicit cross-reference to the priority classification scheme.

- **TG2.2** — **Cross-file search scope incomplete.** ✅ PASSED. Full-text search across all three files confirms no stale binary-only Revise/Redesign references: create.md uses "Redesign" and "Revise" only in their proper four-tier routing contexts (lines 240, 242); polish.md uses "Revise" only in its existing three-verdict context (Accept/Revise/Rethink — this is the review verdict, not the dispatch verdict tier); SKILL.md uses "Revise" only in verdict and revision handoff contexts. No contradictory binary references found.

- **TG2.3** — **SKILL.md dispatch hook table description granularity.** ✅ Acceptable. The `tpm` hook one-line description ("applies decision quality tests (ASR, START, ADMM)") is a summary. The fundamental-vs-addressable routing detail lives in create.md Step 4b where it belongs. The summary-level description remains accurate — the hook still applies decision quality tests. No change needed.

---

## Stage 3: Finalize

### Security Checks

- [x] **S3.1** — No user-supplied strings interpolated into SQL, shell commands, or file paths — N/A: status field change only
- [x] **S3.2** — No secrets, credentials, or API keys in committed files — ✅ Verified: git diff shows only Status line changed, no accidental content
- [x] **S3.3** — No deserialization of untrusted input without validation — N/A
- [x] **S3.4** — Dependencies pinned to specific versions (no wildcards) — N/A
- [x] **S3.5** — File permissions on created artifacts are not overly permissive — N/A
- [x] **S3.6** — Any new external input surface has validation at the boundary — N/A

### UX Checks

- [x] **U3.1** — Every error path produces a human-readable message on stderr — N/A
- [x] **U3.2** — Every user-facing command exits with code 0 on success, non-zero on failure — N/A
- [x] **U3.3** — Invalid input is rejected with a helpful message, not a crash — N/A
- [x] **U3.4** — Resources (file handles, database connections) are cleaned up on error paths — N/A
- [x] **U3.5** — If the stage writes data, there is a way to read it back or verify it — ✅ ADR-0065 line 4 reads "Status: Accepted"
- [x] **U3.6** — If the stage creates state, there is a way to inspect the new state — ✅ Git diff confirms only the Status line changed (Planned → Accepted), no other ADR content modified
- [x] **U3.7** — A user who did not write the code can verify the stage's output — ✅ `grep -c "^Status: Accepted" docs/adr/0065-*.md` returns 1

### Test-Gap Findings

No test-gap findings for Stage 3. The acceptance criteria (status reads "Accepted", no other content modified) are sufficient and mechanically verifiable.

---

## Recommendations

| # | Stage | Classification | Finding | Recommendation | Plan Ref | Status |
|---|-------|----------------|---------|----------------|----------|--------|
| 1 | 1 | Quality concern | TG1.1: "Revise — Major" may use inconsistent formatting (em dash vs hyphen) between Step 4a and Step 4b, causing agent parsing failures | Add a cross-step formatting verification criterion to Task 1.3: "Revise — Major" uses identical character sequence in both steps (U+2014 em dash, not hyphen) | Task 1.3 criteria | ✅ Passed — both occurrences use identical U+2014 em dash (verified via xxd) |
| 2 | 1 | Quality concern | TG1.2: Step 4a step 3 inline routing logic still uses binary Revise/Redesign — adding "Revise — Major" to the verdict table without updating the routing bullets creates a contradiction | Task 1.2 implementation must update the Step 4a step 3 bullet list to include "Revise — Major" routing, not just the verdict table | Task 1.2 criteria | ✅ Passed — Step 4a step 3 bullets include Redesign, Revise — Major, and Revise routing |
| 3 | 1 | Quality concern | TG1.3: Step 4b "not-ready" split into fundamental vs addressable lacks a concrete distinguishing rule — agent cannot reliably route without one | Verify Task 1.3 implementation includes a decision rule (e.g., "missing START criteria or detected anti-patterns → fundamental; structural gaps without anti-patterns → addressable") | Task 1.3 criteria | ✅ Passed — explicit distinguishing rule at line 270 defines fundamental vs addressable |
| 4 | 2 | Quality concern | TG2.1: R-6a dispatch threshold references Low/Medium+ severity, but current review output format may not classify Accept-suggestions by severity level | Verify that the review output format (polish.md Review Phase) classifies suggestions with severity, or that R-6a defines how to assess severity for Accept-with-suggestions findings | Task 2.1 criteria | ⚠️ Deferred — severity mapping is implicit via existing (H)/(M)/(L) priority scheme; does not block execution but could be made explicit in a future pass |
| 5 | 2 | Quality concern | TG2.2: Cross-file consistency check (Task 2.2) should explicitly search all three files for stale binary Revise/Redesign references, not just SKILL.md | QA executor should run a full-text search for "Redesign" and "Revise" (without "Major") across create.md, polish.md, and SKILL.md to verify no stale binary verdict references remain | Task 2.2 criteria | ✅ Passed — full-text search confirms all Revise/Redesign references are in proper contexts |
| 6 | 2 | Preference | TG2.3: SKILL.md dispatch hook table may under-describe the refined tpm verdict routing (fundamental vs addressable not-ready) | Consider whether the hook table's one-line description should mention the split — current "applies decision quality tests" may be sufficient at the summary level | Task 2.2 criteria 2 | ✅ Acceptable — summary-level description is accurate; routing detail lives in create.md Step 4b |
| 7 | 1–2 | Quality concern | ADR-0065 Quality Strategy flags: "Revise — Major" must appear consistently in verdict table, routing logic, and any inline examples across both create.md and polish.md | QA executor should search both files for all occurrences of verdict tier names and verify each occurrence uses the updated four-tier set | ADR-0065 Quality Strategy | ✅ Passed — "Revise — Major" appears in Step 4a and Step 4b routing; polish.md correctly does not use verdict tiers |
| 8 | 2 | Preference | Editor threshold (≤2 Low-severity) is calibrated from a single dogfood session — threshold value may need tuning | ADR-0065 Consequences section already notes this uncertainty as neutral. No action needed now; revisit if broader usage data emerges | Task 2.1, ADR-0065 Neutral Consequences | — |
