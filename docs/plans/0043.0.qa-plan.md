# QA Plan: Replace vendor prefix with remote in wi-nygard-agent naming

**Source Plan:** docs/plans/0043.0.plan.md
**Source ADR:** docs/adr/0043-replace-vendor-prefix-with-remote-in-wi-nygard-agent-naming.md
**Generated:** 2026-04-06
**Location:** `docs/plans/0043.0.qa-plan.md`

---

## Stage 1: Rename vendor → remote in ADR files

### Security Checks

- [x] **S1.1–S1.6** — PASS. This stage edits markdown files only. No code, no input surfaces, no secrets, no dependencies, no file permissions.

### UX Checks

- [x] **U1.1–U1.4** — PASS. N/A for markdown edits.

- [ ] **U1.5** — If the stage writes data, there is a way to read/verify it was written. Task 1.4 (grep audit) serves as the verification mechanism, but only checks for remaining "vendor" instances — it does not verify that replacements are correct (e.g., `{remote}` not `{remotes}`).

- [x] **U1.6–U1.7** — PASS. Files are readable; `git diff` shows changes.

### Test-Gap Findings

1. **No cross-reference integrity check.** The rename changes inline references like `ADR-{vendor}-{id}` → `ADR-{remote}-{id}`. If any ADR cross-references these patterns from *outside* the three target files (e.g., ADR-0036, ADR-0037, ADR-0045), they become inconsistent. The grep audit only checks the three target files.

   **Classification:** Preference — the cross-references are in Proposed ADRs that haven't been implemented. Inconsistency is cosmetic until implementation.

---

## Stage 2: Finalize

### Security Checks

- [x] **S2.1–S2.6** — PASS. Editing a markdown header.

### UX Checks

- [x] **U2.1–U2.7** — PASS. `grep "Status:" docs/adr/0043-*.md` verifies the change.

### Test-Gap Findings

None.

---

## Recommendations

| # | Stage | Classification | Finding | Recommendation |
|---|-------|----------------|---------|----------------|
| 1 | 1 | Preference | Cross-references to `{vendor}` patterns may exist in ADRs outside the three target files (0036, 0037, 0045) | Defer — these ADRs are Proposed and will be updated during their own implementation |
