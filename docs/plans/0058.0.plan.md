# Implementation Plan: Relocate Cargo workspace from crates/ to src/crates/

**Source ADRs:**
- [ADR-0058: Relocate Cargo workspace from crates/ to src/crates/](docs/adr/0058-relocate-cargo-workspace-from-crates-to-src-crates.md)

**Generated:** 2026-04-08
**Location:** `docs/plans/0058.0.plan.md`
**Revision:** 0

---

## Task Execution Protocol

When executing any task from this plan, follow this protocol:

1. **Before starting a task** — read the task's **Test & Acceptance Criteria** section in full.
2. **As each criterion is satisfied** — immediately edit this plan file to mark the checkbox from `- [ ]` to `- [x]`.
3. **After completing all implementation work** for a task — review the criteria list one final time. Any criterion still unchecked is either incomplete work (go back and finish it) or an oversight (check it off with a brief inline note if it was already satisfied by prior work).
4. **Do not batch checkbox updates** to the end of a stage — update them incrementally so that progress is visible in the file at all times.

---

## Stage 1: Directory Relocation

### Task 1.1: Move Cargo workspace to src/crates/    [small]

**Description:** Relocate the Cargo workspace from top-level `crates/` to `src/crates/` using `git mv`. This preserves git history for all files in the workspace.

**Implementation Notes:**
```bash
git mv crates src/crates
```

**Test & Acceptance Criteria:**
- [x] `src/crates/Cargo.toml` exists and is a valid workspace manifest
- [x] `src/crates/adr-db/`, `src/crates/adr-db-lib/`, and `src/crates/adr-format/` exist
- [x] `crates/` no longer exists at the repo root
- [x] `cargo build --release --manifest-path src/crates/Cargo.toml` succeeds

**Dependencies:** None

**ADR Reference:** ADR-0058, Decision §Migration step 1

---

### Task 1.2: Update Makefile build-tools target    [small]

**Description:** Change the `build-tools` Makefile target to reference `src/crates/Cargo.toml` instead of `crates/Cargo.toml`.

**Implementation Notes:**
```makefile
cargo build --release --manifest-path $(CURDIR)/src/crates/Cargo.toml
```

**Test & Acceptance Criteria:**
- [x] `make build-tools` succeeds
- [x] Makefile references `src/crates/` instead of `crates/`

**Dependencies:** Task 1.1

**ADR Reference:** ADR-0058, Decision §Migration step 2

---

### Task 1.3: Update .gitignore    [small]

**Description:** Update `.gitignore` entries that reference `crates/` to reference `src/crates/` instead.

**Test & Acceptance Criteria:**
- [x] `.gitignore` entries reference `src/crates/target/` and `src/crates/adr-db/.env`
- [x] No stale `crates/` patterns remain in `.gitignore`

**Dependencies:** Task 1.1

**ADR Reference:** ADR-0058, Decision §Migration step 4

---

## Stage 2: Reference Updates

### Task 2.1: Update AGENTS.md    [small]

**Description:** Update all P-13 references in AGENTS.md from `crates/` to `src/crates/`. This includes the policy table, section header, prose descriptions, and code examples.

**Test & Acceptance Criteria:**
- [x] P-13 policy table row references `src/crates/`
- [x] Section header updated to reference `src/crates/`
- [x] All prose and code examples in P-13 reference `src/crates/`
- [x] No stale `crates/` path references remain in AGENTS.md (excluding historical mentions of ADR-0028)
- [x] Opportunistic scan: update any actionable `crates/` references in docs/ files that would mislead contributors (historical ADR decisions are left as-is per ADR-0058 §Migration step 6)

**Dependencies:** Stage 1

**ADR Reference:** ADR-0058, Decision §Migration steps 3 and 6

---

### Task 2.2: Update wi-full-agent-adr-format.sh    [small]

**Description:** Fix the relative path to the `adr-format` binary in `src/skills/author-adr/scripts/wi-full-agent-adr-format.sh`. Line 15 currently uses `../../../../crates/target/release/adr-format`. After the move, `crates/` is under `src/`, so the path becomes `../../../crates/target/release/adr-format` (one fewer parent traversal).

**Implementation Notes:**
```bash
# Before: CRATES_BIN="$SCRIPT_DIR/../../../../crates/target/release/adr-format"
# After:  CRATES_BIN="$SCRIPT_DIR/../../../crates/target/release/adr-format"
```

**Test & Acceptance Criteria:**
- [ ] `wi-full-agent-adr-format.sh` line 15 references `../../../crates/target/release/adr-format`
- [ ] The resolved path correctly points to `src/crates/target/release/adr-format`

**Dependencies:** Task 1.1

**ADR Reference:** ADR-0058, Decision §Migration step 5

---

### Task 2.3: Verify test suite    [small]

**Description:** Run the full test suite to confirm nothing is broken by the relocation.

**Test & Acceptance Criteria:**
- [ ] `make test` passes (all 31 tests)
- [ ] `make check-refs` passes
- [ ] `cd src/crates && cargo test` passes

**Dependencies:** Tasks 2.1, 2.2

**ADR Reference:** ADR-0058, Quality Strategy §Build verification, §Test verification

---

## Stage 3: Finalize

### Task 3.1: Update ADR status    [small]

**Description:** Update ADR-0058 from `Planned` to `Accepted` (the ADR will be in `Planned` status at this point — it is transitioned from `Ready` to `Planned` in Step I-5 before execution begins). Confirm ADR-0028 status reads `Superseded by ADR-0058` (it was set during authoring).

**Files to update:**
- `docs/adr/0058-relocate-cargo-workspace-from-crates-to-src-crates.md` — change status from `Planned` to `Accepted`

**Test & Acceptance Criteria:**
- [ ] ADR-0058 status reads `Accepted`
- [ ] ADR-0028 status reads `Superseded by [ADR-0058](...)`
- [ ] No other ADR content is modified

**Dependencies:** All prior stages

**ADR Reference:** ADR-0003, Decision §2

---

## Summary

| Stage | Task | Cost | Dependencies |
|-------|------|------|--------------|
| 1. Directory Relocation | 1.1 Move Cargo workspace | small | — |
| 1. Directory Relocation | 1.2 Update Makefile | small | 1.1 |
| 1. Directory Relocation | 1.3 Update .gitignore | small | 1.1 |
| 2. Reference Updates | 2.1 Update AGENTS.md | small | Stage 1 |
| 2. Reference Updates | 2.2 Update wi-full-agent-adr-format.sh | small | 1.1 |
| 2. Reference Updates | 2.3 Verify test suite | small | 2.1, 2.2 |
| 3. Finalize | 3.1 Update ADR status | small | All prior |

**Total estimated cost:** 7 small
