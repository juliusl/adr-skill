# Implementation Plan: Add option-evaluation dispatch hooks for UX/DX reviewer and TPM integration

**Source ADRs:**
- [ADR-0064: Add option-evaluation dispatch hooks for UX/DX reviewer and TPM integration](docs/adr/0064-add-option-evaluation-dispatch-hooks-for-review-and-decision-arbitration.md)

**Generated:** 2026-04-10
**Location:** `docs/plans/0064.0.plan.md`
**Revision:** 0

---

## Task Execution Protocol

When executing any task from this plan, follow this protocol:

1. **Before starting a task** — read the task's **Test & Acceptance Criteria** section in full.
2. **As each criterion is satisfied** — immediately edit this plan file to mark the checkbox from `- [ ]` to `- [x]`.
3. **After completing all implementation work** for a task — review the criteria list one final time. Any criterion still unchecked is either incomplete work (go back and finish it) or an oversight (check it off with a brief inline note if it was already satisfied by prior work).
4. **Do not batch checkbox updates** to the end of a stage — update them incrementally so that progress is visible in the file at all times.

---

## Stage 1: Dispatch Configuration

### Task 1.1: Add new dispatch keys to SKILL.md    [small]

**Description:** Update the Agent Dispatch section in `src/skills/author-adr/SKILL.md` to document three new dispatch keys: `ux_review`, `dx_review`, and `tpm`. Add them to the TOML example block, the hook table, and the description text.

**Implementation Notes:**

In `src/skills/author-adr/SKILL.md`, locate the `### Agent Dispatch` section (around line 125). Make these changes:

1. Update the description text from "three hook points" to "six hook points" and add "option evaluation (Step 4a/4b)" to the list.
2. Add the three new keys to the TOML example block:
   ```toml
   ux_review = ""             # Agent for UX option review (default: skip)
   dx_review = ""             # Agent for DX option review (default: skip)
   tpm = ""                   # Agent for decision quality assessment (default: skip)
   ```
3. Add three rows to the hook table:
   | `ux_review` | `""` (skip) | UX Reviewer | Dispatched at Step 4a — evaluates options for user experience quality |
   | `dx_review` | `""` (skip) | DX Reviewer | Dispatched at Step 4a — evaluates options for developer experience quality |
   | `tpm` | `""` (skip) | Decision Arbiter | Dispatched at Step 4b — applies decision quality tests (ASR, START, ADMM) |
4. After the hook table, add: "For `ux_review`, `dx_review`, and `tpm`, the empty string `""` means the hook is skipped — no dispatch occurs. Any non-empty value is treated as an agent reference."

**Test & Acceptance Criteria:**
- [x] TOML example contains all 6 keys (review, editor, tech_writer, ux_review, dx_review, tpm)
- [x] Hook table has 6 rows with correct defaults and descriptions
- [x] Description text mentions six hook points
- [x] SKILL.md remains under 500 lines

**Dependencies:** None

**ADR Reference:** ADR-0064, Decision (Option A — new dispatch keys)

---

### Task 1.2: Update A-0 to read new dispatch keys    [small]

**Description:** Update the A-0 Format Detection step in `src/skills/author-adr/SKILL.md` to read the three new dispatch keys (`ux_review`, `dx_review`, `tpm`) alongside the existing keys.

**Implementation Notes:**

In `src/skills/author-adr/SKILL.md`, locate A-0 step 1 (around line 149). Update the instruction:

Change:
```
Also read `[author.dispatch]` keys (`review`, `editor`, `tech_writer`) if present. Store for use during create, review, and revise workflows. If absent, use defaults (`review = "general-purpose"`, `editor = "interactive"`, `tech_writer = ""`).
```

To:
```
Also read `[author.dispatch]` keys (`review`, `editor`, `tech_writer`, `ux_review`, `dx_review`, `tpm`) if present. Store for use during create, review, and revise workflows. If absent, use defaults (`review = "general-purpose"`, `editor = "interactive"`, `tech_writer = ""`, `ux_review = ""`, `dx_review = ""`, `tpm = ""`).
```

**Test & Acceptance Criteria:**
- [ ] A-0 step 1 lists all 6 dispatch keys
- [ ] Default values for new keys are `""` (empty string = skip)

**Dependencies:** Task 1.1

**ADR Reference:** ADR-0064, Decision (Option A — configuration)

---

## Stage 2: Evaluation Checkpoint Integration

### Task 2.1: Update procedure table and flow diagram in create.md    [small]

**Description:** Update the procedure table and flow diagram in `src/skills/author-adr/references/create.md` to include the new Step 4a and Step 4b sub-steps.

**Implementation Notes:**

In `src/skills/author-adr/references/create.md`:

1. Add to the procedure table (after the Step 4 row):
   ```
   | Step 4a | UX/DX Option Review (conditional) — dispatch reviewers on Options |
   | Step 4b | TPM Decision Quality Assessment (conditional) — dispatch TPM with findings |
   ```

2. Update the flow diagram to show the new sub-steps:
   ```
   Step 4 — Evaluation Checkpoint (conditional: Proceed / Pause / Skip)
     ├─ Step 4a — UX/DX Option Review (conditional: ux_review or dx_review configured?)
     └─ Step 4b — TPM Decision Quality Assessment (conditional: tpm configured?)
   ```

**Test & Acceptance Criteria:**
- [ ] Procedure table includes Step 4a and Step 4b entries
- [ ] Flow diagram shows Step 4a and Step 4b as sub-steps of Step 4
- [ ] Both sub-steps are marked as conditional

**Dependencies:** None

**ADR Reference:** ADR-0064, Decision (Option A — Step 4a, Step 4b)

---

### Task 2.2: Add Step 4a UX/DX Option Review to create.md    [medium]

**Description:** Add the Step 4a sub-section to `src/skills/author-adr/references/create.md` inside the Step 4 (Evaluation Checkpoint) section. This step dispatches configured UX and DX review agents in parallel on the Options section.

**Implementation Notes:**

Add after the existing Step 4 content (before Step 5), a new sub-section:

```markdown
### Step 4a: UX/DX Option Review (Conditional)

**Condition:** At least one of `[author.dispatch].ux_review` or `[author.dispatch].dx_review` is configured with a non-empty value.

When UX or DX review agents are configured, dispatch them to evaluate the Options section before the decision converges.

1. **Build dispatch context** — assemble the payload for each configured reviewer:
   - The ADR file path (with Options section populated)
   - The artifact to review: the Options section content
   - Scope: `New` (options are being authored, not modified)

2. **Dispatch in parallel** — invoke each configured agent via the `task` tool simultaneously. Each agent runs its standard review procedure and returns findings using its established output format.

3. **Incorporate findings** — after both agents return, incorporate their findings into the checkpoint assessment:
   - If either agent returns a **Redesign** verdict, set the checkpoint Assessment to `Pause for validation` and populate Validation needs with the findings.
   - If either agent returns a **Revise** verdict, present Medium findings as checkpoint considerations — they inform the decision but do not block it.
   - If both return **Accept** or **Accept with suggestions**, proceed normally.

4. **Fallback** — if a configured agent cannot be resolved at runtime, warn and skip that agent. If all configured agents fail to resolve, skip Step 4a.

**When neither `ux_review` nor `dx_review` is configured:** Skip Step 4a. Log: "Step 4a skipped — no UX/DX review agents configured."
```

**Test & Acceptance Criteria:**
- [ ] Step 4a sub-section exists in create.md under Step 4
- [ ] Condition clearly states when the step activates (non-empty ux_review or dx_review)
- [ ] Dispatch describes parallel execution via task tool
- [ ] Finding incorporation maps reviewer verdicts to checkpoint assessment values
- [ ] Fallback behavior specified for unresolvable agents
- [ ] Skip behavior documented for unconfigured hooks

**Dependencies:** Task 2.1

**ADR Reference:** ADR-0064, Decision (Option A — Step 4a)

---

### Task 2.3: Add Step 4b TPM Decision Quality Assessment to create.md    [medium]

**Description:** Add the Step 4b sub-section to `src/skills/author-adr/references/create.md` inside the Step 4 (Evaluation Checkpoint) section. This step dispatches the configured TPM agent with the ADR content and any UX/DX findings.

**Implementation Notes:**

Add after Step 4a, before Step 5:

```markdown
### Step 4b: TPM Decision Quality Assessment (Conditional)

**Condition:** `[author.dispatch].tpm` is configured with a non-empty value.

When a TPM agent is configured, dispatch it to assess decision quality at the Evaluation Checkpoint.

1. **Build dispatch context** — assemble the payload for the TPM:
   - The ADR file path (with Context, Options, and Decision Drivers populated)
   - UX/DX review findings from Step 4a (if available — UX/DX findings are optional enrichment, not a prerequisite)
   - Instruction: apply ASR, START, and ADMM tests; detect anti-patterns; validate justification readiness

2. **Dispatch via `task` tool** — invoke the configured TPM agent.

3. **Incorporate assessment** — after the TPM returns:
   - If the TPM flags **not-ready** (missing START criteria, detected anti-patterns, or fallacies), set the checkpoint Assessment to `Pause for validation` and populate Validation needs with the TPM's findings.
   - If the TPM flags **ready with findings**, present findings as checkpoint considerations and proceed with `Proceed`.
   - If the TPM flags **ready**, proceed normally.

4. **Fallback** — if the configured agent cannot be resolved at runtime, fall back to the inline agent's existing checkpoint assessment and warn the user.

**When `tpm` is not configured:** Skip Step 4b. Log: "Step 4b skipped — no TPM agent configured."
```

**Test & Acceptance Criteria:**
- [ ] Step 4b sub-section exists in create.md under Step 4, after Step 4a
- [ ] Condition clearly states when the step activates (non-empty tpm)
- [ ] Dispatch context includes ADR content and optional UX/DX findings
- [ ] TPM assessment maps to checkpoint Assessment values (Proceed/Pause)
- [ ] Fallback behavior specified for unresolvable agent
- [ ] Skip behavior documented for unconfigured hook

**Dependencies:** Task 2.2

**ADR Reference:** ADR-0064, Decision (Option A — Step 4b)

---

## Stage 3: Finalize

### Task 3.1: Validate changes    [small]

**Description:** Run the project's test suite, check references, and verify SKILL.md line count to ensure changes do not break existing behavior.

**Implementation Notes:**

```bash
make test                    # 32 tests must pass
make check-refs              # all references must resolve
make validate                # validate author-adr skill spec
wc -l src/skills/author-adr/SKILL.md  # must be < 500 lines
```

**Test & Acceptance Criteria:**
- [ ] `make test` passes (32 tests)
- [ ] `make check-refs` passes (zero broken references)
- [ ] `make validate` passes (skill spec validation)
- [ ] SKILL.md is under 500 lines

**Dependencies:** Stage 1, Stage 2

**ADR Reference:** AGENTS.md P-7, P-8

---

### Task 3.2: Update ADR status to Accepted    [small]

**Description:** Update ADR-0064 status from `Planned` to `Accepted` to reflect that the decision has been fully implemented.

**Files to update:**
- `docs/adr/0064-add-option-evaluation-dispatch-hooks-for-review-and-decision-arbitration.md` — change status from `Planned` to `Accepted`

**Test & Acceptance Criteria:**
- [ ] ADR-0064 status reads `Accepted`
- [ ] No other ADR content is modified

**Dependencies:** Task 3.1

**ADR Reference:** ADR-0003, Decision §2

---

## Summary

| Stage | Task | Cost | Dependencies |
|-------|------|------|--------------|
| 1. Dispatch Configuration | 1.1 Add new dispatch keys to SKILL.md | small | — |
| 1. Dispatch Configuration | 1.2 Update A-0 to read new keys | small | 1.1 |
| 2. Checkpoint Integration | 2.1 Update procedure table and flow diagram | small | — |
| 2. Checkpoint Integration | 2.2 Add Step 4a UX/DX Option Review | medium | 2.1 |
| 2. Checkpoint Integration | 2.3 Add Step 4b TPM Decision Quality Assessment | medium | 2.2 |
| 3. Finalize | 3.1 Validate changes | small | Stage 1, 2 |
| 3. Finalize | 3.2 Update ADR status to Accepted | small | 3.1 |

**Total estimated cost:** 5 small, 2 medium, 0 heavy

**Accepted tradeoffs:**
- Dispatch key density grows from 3→6 in `[author.dispatch]`. At 8+ keys, a sub-table becomes warranted per ADR-0014. No mitigation task — this is a future concern, not an immediate risk at 6 keys.
