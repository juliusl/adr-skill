# Implementation Plan: Run TPM Dispatch Parallel with UX/DX Review

**Source ADRs:**
- [ADR-0066: Run TPM dispatch parallel with UX/DX review](docs/adr/0066-run-tpm-dispatch-parallel-with-ux-dx-review.md)

**Generated:** 2026-04-10
**Location:** `docs/plans/0066.0.plan.md`
**Revision:** 0

---

## Task Execution Protocol

When executing any task from this plan, follow this protocol:

1. **Before starting a task** — read the task's **Test & Acceptance Criteria** section in full.
2. **As each criterion is satisfied** — immediately edit this plan file to mark the checkbox from `- [ ]` to `- [x]`.
3. **After completing all implementation work** for a task — review the criteria list one final time. Any unchecked criterion is either incomplete work or an oversight — finish it, or check it off with a brief inline note.
4. **Do not batch checkbox updates** to the end of a stage — update incrementally.

---

## Stage 1: Parallel Dispatch Implementation

### Task 1.1: Update create.md — parallelize Step 4a and 4b    [small]

**Description:** Modify `src/skills/author-adr/references/create.md` to make Step 4b run in parallel with Step 4a instead of sequentially. Remove the sequential dependency language from Step 4b and the flow diagram. Add combined verdict resolution logic after both steps return.

**Implementation Notes:**

1. In the procedure table (line 95), remove "with findings" from Step 4b description — TPM no longer receives 4a findings.
2. In the flow diagram (lines 110–111), change from sequential to parallel notation:
   ```
   Step 4 — Evaluation Checkpoint (conditional: Proceed / Pause / Skip)
     ├─ Step 4a — UX/DX Option Review (conditional: ux_review or dx_review configured?)
     ├─ Step 4b — TPM Decision Quality Assessment (conditional: tpm configured?)
     └─ (4a and 4b dispatch in parallel; consolidate findings after both return)
   ```
3. In the conditional steps note (line 118), replace "Step 4b is conditional on `tpm` being configured and runs after Step 4a, consuming Step 4a's findings when available" with "Step 4b is conditional on `tpm` being configured. Steps 4a and 4b dispatch in parallel when both are configured; findings are consolidated after both return."
4. In Step 4b body (line 258), remove "UX/DX review findings from Step 4a" from the dispatch context list.
5. Add a new section after Step 4b: "### Step 4 Verdict Consolidation" that describes how to merge verdicts from 4a and 4b when both run.

**Test & Acceptance Criteria:**
- [x] Step 4b no longer references Step 4a's findings as input [implement]
- [x] Flow diagram shows 4a and 4b as parallel, not sequential [implement]
- [x] Conditional steps note describes parallel dispatch [implement]
- [x] Verdict consolidation logic defined (Redesign > Revise — Major > Revise > Accept) [implement]
- [x] `make check-refs` passes [implement]

**Dependencies:** None

**ADR Reference:** ADR-0066, Decision §1–§4

---

### Task 1.2: Update SKILL.md — align dispatch table and A-0 description    [small]

**Description:** Update `src/skills/author-adr/SKILL.md` to reflect that Step 4b now runs in parallel with Step 4a. Update the dispatch table description for `tpm` and any procedure-level text referencing the sequential relationship.

**Implementation Notes:**

1. Line 153: Change `tpm` description from "Dispatched at Step 4b" to "Dispatched at Step 4 (parallel with 4a)".
2. Any text describing 4b as running "after" 4a should be updated to "alongside" or "in parallel with".

**Test & Acceptance Criteria:**
- [ ] `tpm` dispatch table entry describes parallel dispatch [implement]
- [ ] No remaining references to 4b running "after" 4a in SKILL.md [implement]
- [ ] `make check-refs` passes [implement]

**Dependencies:** None

**ADR Reference:** ADR-0066, Decision §1

---

### Task 1.3: Update bundled ADR-0064 asset    [small]

**Description:** Update `src/skills/author-adr/assets/decisions/0064-*.md` to annotate that Steps 4a/4b are now parallel per ADR-0066, consistent with how ADR-0031 was annotated for the editor removal.

**Implementation Notes:**

Add an annotation in the ADR-0064 asset where the sequential 4a→4b relationship is described:
```markdown
<!-- ADR-0066: Steps 4a and 4b now dispatch in parallel. The TPM no longer
     receives UX/DX findings as input. Findings are consolidated after both return. -->
```

**Test & Acceptance Criteria:**
- [ ] ADR-0064 asset reflects parallel dispatch with annotation [implement]
- [ ] No contradictory sequential language remains in the asset [implement]

**Dependencies:** None

**ADR Reference:** ADR-0066, Decision §3

---

## Stage 2: Resolve TODOS.md Items

### Task 2.1: Close TODOS items #12 and #13    [small]

**Description:** Strike through TODOS.md items #12 and #13 — both are resolved by the parallel dispatch change. Item #12 (execution order ambiguity) is eliminated because there is no sequential dependency. Item #13 (Step 4a Redesign not gating 4b) is eliminated because both run independently.

**Test & Acceptance Criteria:**
- [ ] Items #12 and #13 have strikethrough formatting [implement]
- [ ] Triage column updated to "Done — resolved by ADR-0066" [implement]

**Dependencies:** Stage 1

**ADR Reference:** ADR-0066, Consequences

---

## Stage 3: Validate

### Task 3.1: Run test suite and check-refs    [small]

**Description:** Run `make test` and `make check-refs` to verify no regressions.

**Test & Acceptance Criteria:**
- [ ] All tests pass [implement]
- [ ] All references valid across all skills [implement]

**Dependencies:** Stage 2

**ADR Reference:** ADR-0066, Quality Strategy (Unit testing)

---

## Stage 4: Finalize

### Task 4.1: Update ADR status and Deliverables    [small]

**Description:** Update ADR-0066 status from `Proposed` to `Accepted`.

**Files to update:**
- `docs/adr/0066-run-tpm-dispatch-parallel-with-ux-dx-review.md` — change `## Status` from `Proposed` to `Accepted`

**Test & Acceptance Criteria:**
- [ ] ADR-0066 status reads `Accepted`
- [ ] User documentation checkbox in Quality Strategy marked `[x]` (Tasks 1.1/1.2 updated the docs)
- [ ] No other ADR content is modified

**Dependencies:** All prior stages

**ADR Reference:** ADR-0003, Decision §2

---

## Summary

| Stage | Task | Cost | Dependencies |
|-------|------|------|--------------|
| 1. Parallel Dispatch | 1.1 Update create.md | small | — |
| 1. Parallel Dispatch | 1.2 Update SKILL.md | small | — |
| 1. Parallel Dispatch | 1.3 Update ADR-0064 asset | small | — |
| 2. TODOS Cleanup | 2.1 Close items #12, #13 | small | Stage 1 |
| 3. Validate | 3.1 Run tests and check-refs | small | Stage 2 |
| 4. Finalize | 4.1 Update ADR status | small | All prior |

**Total estimated cost:** 6 small
