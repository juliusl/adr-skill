# QA Plan: Support list of code review agents in solve-adr dispatch

**Source Plan:** docs/plans/0060.0.plan.md
**Generated:** 2026-04-08
**Location:** `docs/plans/0060.0.qa-plan.md`

---

## Stage 1: Config Schema and Parsing

This stage modifies documentation only (SKILL.md). Security and UX checklists are evaluated for applicability — most items are N/A for prose changes but are explicitly visited per protocol.

### Security Checks

- [x] **S1.1** — No user-supplied strings interpolated into SQL, shell commands, or file paths — N/A (documentation-only change; no executable code modified). TOML examples (lines 102-106) and normalization prose (line 153) contain no injection-enabling patterns. **PASS**
- [x] **S1.2** — No secrets, credentials, or API keys in committed files — TOML snippets use agent reference names (e.g., `juliusl-code-reviewer-v1`), not credentials. **PASS**
- [x] **S1.3** — No deserialization of untrusted input without validation — N/A (no code). Normalization prose specifies filtering: line 113 ("Empty strings and whitespace-only entries are filtered out") and line 153 ("Filter out empty or whitespace-only entries"). **PASS**
- [x] **S1.4** — Dependencies pinned to specific versions — N/A (no dependencies added). **PASS**
- [x] **S1.5** — File permissions on created artifacts are not overly permissive — N/A (no files created at runtime). **PASS**
- [x] **S1.6** — Any new external input surface has validation at the boundary — Line 113 documents all three cases: bare string normalized to list, empty/whitespace entries filtered, resulting empty list → skip C-1. Line 153 repeats normalization in S-0 step 4. **PASS**

### UX Checks

- [x] **U1.1** — Every error path produces a human-readable message — C-1a (line 261) specifies: `Log: "C-1 skipped — no code review agents configured."` S-0 step 4 normalizes silently (appropriate — the skip message fires at the action point in C-1a). **PASS**
- [x] **U1.2** — Every user-facing command exits with code 0 on success, non-zero on failure — N/A (no commands added). **PASS**
- [x] **U1.3** — Invalid input is rejected with a helpful message — Whitespace-only strings: documented (line 113, "whitespace-only entries are filtered out"). Non-string array elements: not explicitly addressed, but implicitly covered by "array of strings" type description. TOML parsing would surface type mismatches at the parser level. **PASS**
- [x] **U1.4** — Resources cleaned up on error paths — N/A (documentation-only). **PASS**
- [x] **U1.5** — If the stage writes data, there is a way to read it back — N/A (documentation-only). **PASS**
- [x] **U1.6** — If the stage creates state, there is a way to inspect it — N/A (documentation-only). **PASS**
- [x] **U1.7** — A user who did not write the code can verify the stage's output — Flow is self-consistent: config (line 113) defines the key → S-0 step 4 (line 153) normalizes to list → C-1a (line 261) checks the list → C-1c (line 265) dispatches each reviewer. No contradictions found. **PASS**

### Test-Gap Findings

- **TG1.1** — **Config-to-C-1 consistency gap.** ✅ **PASS** — Terminology is consistent across sections. Config (line 113): "the resulting list." S-0 step 4 (line 153): "Normalize `code_review` to a list" / "the resulting list." C-1a (line 261): "the normalized `code_review` list from S-0." C-1c-f use "configured reviewer(s)" / "all reviewers" — contextually clear references to entries in the normalized list.

- **TG1.2** — **Summary table staleness.** ✅ **PASS** — All 7 rows in the C-1 summary table (lines 245-255) use plural or list-aware language: C-1 "reviewer(s)", C-1a "list", C-1c "all configured reviewers", C-1d "all reviewers", C-1e "all reviewers", C-1f "all reviewers." C-1b has no reviewer reference (N/A).

- **TG1.3** — **Backward-compatible phrasing completeness.** ✅ **PASS** — Line 113 preserves skip-when-empty: default is `""` (skip), "Empty strings and whitespace-only entries are filtered out," "When the resulting list is empty, C-1 is skipped." C-1a (line 261) also covers absent case: "absent, all entries empty or whitespace-only."

- **TG1.4** — **Line count regression.** ✅ **PASS** — `wc -l` reports 357 lines after both tasks. Well under 500.

---

## Stage 2: Finalize

### Security Checks

- [ ] **S2.1** — No user-supplied strings interpolated into SQL, shell commands, or file paths — N/A (ADR status change only).
- [ ] **S2.2** — No secrets, credentials, or API keys in committed files — N/A.
- [ ] **S2.3** — No deserialization of untrusted input without validation — N/A.
- [ ] **S2.4** — Dependencies pinned to specific versions — N/A.
- [ ] **S2.5** — File permissions on created artifacts are not overly permissive — N/A.
- [ ] **S2.6** — Any new external input surface has validation at the boundary — N/A.

### UX Checks

- [ ] **U2.1** — Every error path produces a human-readable message — N/A.
- [ ] **U2.2** — Every user-facing command exits with code 0 on success, non-zero on failure — N/A.
- [ ] **U2.3** — Invalid input is rejected with a helpful message — N/A.
- [ ] **U2.4** — Resources cleaned up on error paths — N/A.
- [ ] **U2.5** — If the stage writes data, there is a way to read it back — N/A.
- [ ] **U2.6** — If the stage creates state, there is a way to inspect it — N/A.
- [ ] **U2.7** — A user who did not write the code can verify the stage's output — Verify ADR-0060 status field reads `Accepted` and no other content was modified.

### Test-Gap Findings

No test-gap findings for Stage 2. The dev acceptance criteria ("ADR-0060 status reads Accepted" and "no other ADR content is modified") are sufficient and mechanically verifiable.

---

## Recommendations

| # | Stage | Classification | Finding | Recommendation | Plan Ref | Status |
|---|-------|----------------|---------|----------------|----------|--------|
| 1 | 1 | Quality concern | **TG1.1:** Config section and C-1 substeps may use inconsistent terminology for the normalized reviewer list | After both Task 1.1 and Task 1.2, verify that S-0 step 4, C-1a, C-1c, C-1d, C-1e, and C-1f all refer to the normalized list with the same term (e.g., "the reviewer list" or "the normalized list") | Task 1.1 criterion 3, Task 1.2 criterion 1 | ✅ Pass |
| 2 | 1 | Quality concern | **TG1.2:** Summary table row descriptions may remain singular after Task 1.2 edits | Verify every C-1 summary table row that mentions the reviewer uses plural or list-aware language — check all 6 rows (C-1, C-1a through C-1f) | Task 1.2 criterion 6 | ✅ Pass |
| 3 | 1 | Quality concern | **TG1.3:** Config key description rewrite may drop the skip-when-empty behavior | Verify the updated `code_review` key description still documents: absent → skip, empty string → skip, whitespace-only → skip, in addition to the new array semantics | Task 1.1 criterion 2 | ✅ Pass |
| 4 | 1 | Quality concern | **TG1.4:** Line count must be verified after both tasks, not just Task 1.1 | Run `wc -l` on SKILL.md after Task 1.2 completes; `make check-refs` after Task 1.2 as well | Task 1.1 criterion 5 | ✅ Pass |
| 5 | 1 | Preference | **S1.6 supplement:** Config docs could explicitly enumerate the three parsing cases (absent, string, array) as a numbered list for clarity | Consider a 3-case list in the key description rather than inline prose | Task 1.1 criterion 2 | — Deferred |