# QA Plan: Codify Policy Organization for Skill Files

**Source Plan:** docs/plans/0054.0.plan.md
**Generated:** 2026-04-07
**Location:** `docs/plans/0054.0.qa-plan.md`

---

## Stage 1: prototype-adr — Rename Step Identifiers and Add Policies Section

### Security Checks

- [x] **S1.1** — No user-supplied strings interpolated into SQL, shell commands, or file paths — N/A: documentation-only changes, no executable code modified
- [x] **S1.2** — No secrets, credentials, or API keys in committed files — N/A: SKILL.md contains no credentials
- [x] **S1.3** — No deserialization of untrusted input without validation — N/A: no input processing
- [x] **S1.4** — Dependencies pinned to specific versions — N/A: no dependency changes
- [x] **S1.5** — File permissions on created artifacts are not overly permissive — N/A: editing existing file, no new artifacts
- [x] **S1.6** — Any new external input surface has validation at the boundary — N/A: no new input surfaces

### UX Checks

- [x] **U1.1** — Every error path produces a human-readable message on stderr — N/A: no executable code
- [x] **U1.2** — Every user-facing command exits with code 0 on success, non-zero on failure — N/A: no commands
- [x] **U1.3** — Invalid input is rejected with a helpful message, not a crash — N/A: no input processing
- [x] **U1.4** — Resources cleaned up on error paths — N/A: no resources
- [x] **U1.5** — If the stage writes data, there is a way to read it back or verify it — Pass criteria: `cat src/skills/prototype-adr/SKILL.md` shows the restructured file with new identifiers and Policies section
- [x] **U1.6** — If the stage creates state, there is a way to inspect the new state — Pass criteria: `grep -c 'E-[0-6]' src/skills/prototype-adr/SKILL.md` returns 14 (7 table rows + 7 headers); `grep -c 'P-[0-6]' src/skills/prototype-adr/SKILL.md` returns 0
- [x] **U1.7** — A user who did not write the code can verify the stage's output — Pass criteria: diff between old and new SKILL.md shows only identifier renaming (P→E) and Policies section insertion; no procedure content changes

### Test-Gap Findings

- **TG1.1** — **Cross-file anchor breakage not covered by dev acceptance criteria.** `docs/PREFERENCES.md:69` contains a link to `prototype-adr/SKILL.md#p-1-select-isolation`. Renaming `### P-1: Select Isolation` to `### E-1: Select Isolation` changes the anchor from `#p-1-select-isolation` to `#e-1-select-isolation`, breaking this link. The dev plan's Task 1.1 implementation notes claim "No cross-file references exist (confirmed by grep) — changes are contained to SKILL.md" — this is incorrect. The grep only searched within `src/skills/prototype-adr/`, missing links from `docs/PREFERENCES.md`. **Verification command:** `grep -rn '#p-[0-6]' docs/ src/ --include='*.md'` should return 0 hits after remediation.

- **TG1.2** — **`make check-refs` does not validate anchors.** The `scripts/check-refs` tool strips anchor fragments (`filepath="${target%%#*}"`) and only checks file existence. Even if the executor runs `make check-refs`, it will not detect the broken anchor in TG1.1. The broken anchor can only be caught by manual grep or an anchor-aware link checker. The dev plan's Stage 4 Task 4.1 criteria include `make test` and `wc -l` but not a repo-wide anchor validation. **Recommended verification:** `grep -rn 'prototype-adr/SKILL.md#p-' docs/ src/ --include='*.md'` after Stage 1 completes.

- **TG1.3** — **Identifier scheme consistency not validated.** The dev plan suggests `E-0`–`E-6` ("Experiment" prefix) but the acceptance criteria only verify absence of P-prefixed identifiers — they don't verify the replacement scheme is internally consistent (all same prefix, sequential numbering, table entries match headers). A partial rename (e.g., table says `E-3` but header says `EX-3`) would pass all three dev acceptance criteria.

---

## Stage 2: author-adr — Add Policies Section and Elevate Inline Policies

### Security Checks

- [x] **S2.1** — No user-supplied strings interpolated into SQL, shell commands, or file paths — N/A: documentation-only
- [x] **S2.2** — No secrets, credentials, or API keys in committed files — N/A: no credentials
- [x] **S2.3** — No deserialization of untrusted input without validation — N/A: no input processing
- [x] **S2.4** — Dependencies pinned to specific versions — N/A: no dependency changes
- [x] **S2.5** — File permissions on created artifacts are not overly permissive — N/A: editing existing file
- [x] **S2.6** — Any new external input surface has validation at the boundary — N/A: no new input surfaces

### UX Checks

- [x] **U2.1** — Every error path produces a human-readable message on stderr — N/A: no executable code
- [x] **U2.2** — Every user-facing command exits with code 0 on success, non-zero on failure — N/A: no commands
- [x] **U2.3** — Invalid input is rejected with a helpful message, not a crash — N/A: no input processing
- [x] **U2.4** — Resources cleaned up on error paths — N/A: no resources
- [x] **U2.5** — If the stage writes data, there is a way to read it back or verify it — Pass criteria: `cat src/skills/author-adr/SKILL.md` shows Policies section with P-1 and P-2; original inline locations contain back-references
- [x] **U2.6** — If the stage creates state, there is a way to inspect the new state — Pass criteria: `grep -n '## Policies' src/skills/author-adr/SKILL.md` returns a line number less than the `## Procedure` line number
- [x] **U2.7** — A user who did not write the code can verify the stage's output — Pass criteria: diff shows policy text moved from inline locations to Policies section, with back-references at original locations

### Test-Gap Findings

- **TG2.1** — **Semantic content preservation during elevation.** The dev plan's acceptance criterion "No policy content is lost — full text preserved in the Policies section" is a subjective check. Concrete risk: the inline dispatch compliance paragraph (line 118) includes specific context about "autonomous workflows triggered by other skills (e.g., implement-adr invoking author-adr for ADR creation)." The elevated P-1 text in the plan includes this sentence — but the acceptance criteria don't specify a mechanical verification method (e.g., a diff showing the original paragraph content exists in the new location). **Recommended verification:** After elevation, extract the original paragraph text and confirm every sentence appears in the P-1 section. Specifically verify: (a) "always use the configured agent," (b) "do not substitute `general-purpose` or skip dispatch," (c) "applies in all modes, including autonomous workflows."

- **TG2.2** — **Back-reference format consistency.** The dev plan specifies two different back-reference formats: line 118 becomes "See P-1 (Mandatory Dispatch Compliance)" while line 200 becomes "**Guardrail (P-2):** See P-2 (Cross-ADR Modification Guardrail)." The inconsistency is intentional (line 200 already has a `**Guardrail:**` prefix), but the dev acceptance criteria don't verify the back-references are formatted correctly or that they actually point to valid policy IDs. A typo like "See P-3" (nonexistent) would pass the "contains back-references" criterion.

- **TG2.3** — **Document structure integrity after insertion.** The plan inserts ~20 lines between line 10 (skill description) and line 11 (`## Procedure`). The dev acceptance criteria verify section ordering (Policies before Procedure) but don't verify that no existing content is disrupted — specifically that the `## Assets` table (lines 25–32) and the `## Procedure` table (lines 13–22) remain intact. **Recommended verification:** After insertion, confirm `## Procedure` table still has 7 rows (A-0 through A-6) and `## Assets` table entries resolve to existing files.

- **TG2.4** — **Line count after insertion.** Author-adr SKILL.md is currently 296 lines. Adding the Policies section (~20 lines) nets to approximately 316 lines (inline text replaced by shorter back-references partially offsets). This is under the 500-line limit, but should be verified explicitly since the dev plan's Task 4.1 criteria check line counts only in Stage 4, not at the stage boundary.

---

## Stage 3: implement-adr — Evaluate Inline Enforcement for Elevation

### Security Checks

- [x] **S3.1** — No user-supplied strings interpolated into SQL, shell commands, or file paths — N/A: evaluation-only, no file changes
- [x] **S3.2** — No secrets, credentials, or API keys in committed files — N/A: no changes
- [x] **S3.3** — No deserialization of untrusted input without validation — N/A: no changes
- [x] **S3.4** — Dependencies pinned to specific versions — N/A: no changes
- [x] **S3.5** — File permissions on created artifacts are not overly permissive — N/A: no changes
- [x] **S3.6** — Any new external input surface has validation at the boundary — N/A: no changes

### UX Checks

- [x] **U3.1** — Every error path produces a human-readable message on stderr — N/A: no executable code
- [x] **U3.2** — Every user-facing command exits with code 0 on success, non-zero on failure — N/A: no commands
- [x] **U3.3** — Invalid input is rejected with a helpful message, not a crash — N/A: no input processing
- [x] **U3.4** — Resources cleaned up on error paths — N/A: no resources
- [x] **U3.5** — If the stage writes data, there is a way to read it back or verify it — N/A: no data written (evaluation-only stage)
- [x] **U3.6** — If the stage creates state, there is a way to inspect the new state — N/A: no state changes
- [x] **U3.7** — A user who did not write the code can verify the stage's output — Pass criteria: evaluation results documented as inline comments in the plan file with per-statement analysis. Verified: HTML comment at plan.md lines 140–160 contains a 13-row table with line/statement/disposition/rationale columns.

### Test-Gap Findings

- **TG3.1** — **Evaluation completeness is not mechanically verifiable.** The dev plan pre-determines the outcome ("no new policies needed") and the acceptance criteria require confirming "existing P-1–P-3 are sufficient." This is a vacuous pass — the criterion is satisfied by asserting the conclusion without documenting the evidence. The criteria should require listing each inline enforcement statement evaluated, stating why it is step-specific (not cross-cutting), and confirming no cross-step references exist. **Recommended verification:** The executor should enumerate all enforcement-language patterns in implement-adr SKILL.md (grep for "must," "mandatory," "violation," "never") and document the disposition of each.

---

## Stage 4: Verification and Finalize

### Security Checks

- [x] **S4.1** — No user-supplied strings interpolated into SQL, shell commands, or file paths — N/A: verification-only
- [x] **S4.2** — No secrets, credentials, or API keys in committed files — Verified: `grep -rniE '(password|secret|token|api.key|credential)'` across all 4 SKILL.md files returns no matches
- [x] **S4.3** — No deserialization of untrusted input without validation — N/A
- [x] **S4.4** — Dependencies pinned to specific versions — N/A
- [x] **S4.5** — File permissions on created artifacts are not overly permissive — N/A
- [x] **S4.6** — Any new external input surface has validation at the boundary — N/A

### UX Checks

- [x] **U4.1** — Every error path produces a human-readable message on stderr — N/A
- [x] **U4.2** — Every user-facing command exits with code 0 on success, non-zero on failure — N/A
- [x] **U4.3** — Invalid input is rejected with a helpful message, not a crash — N/A
- [x] **U4.4** — Resources cleaned up on error paths — N/A
- [x] **U4.5** — If the stage writes data, there is a way to read it back or verify it — Verified: `make test` exits 0, all 31 tests pass (22 adr-tools + 9 madr-tools)
- [x] **U4.6** — If the stage creates state, there is a way to inspect the new state — Verified: all 4 SKILL.md files have `## Policies` before `## Procedure`: author=12/29, implement=12/38, prototype=14/18, solve=12/55
- [x] **U4.7** — A user who did not write the code can verify the stage's output — Verified: `wc -l` returns 314, 471, 146, 238 (all under 500); `grep -n '## Policies'` returns lines in all 4 files; `make test` passes

### Test-Gap Findings

- **TG4.1** — **`make check-refs` absent from verification criteria.** Task 4.1 acceptance criteria include `make test` and `wc -l` but not `make check-refs`. Per AGENTS.md P-8 ("After Making Changes"), `make check-refs` is a required post-change step. Missing it means broken references introduced by Stage 1 or Stage 2 would not be caught — though as noted in TG1.2, `make check-refs` doesn't validate anchors anyway.

- **TG4.2** — **Repo-wide anchor integrity not verified.** No acceptance criterion in any stage performs a repo-wide search for links that reference renamed anchors. After Stage 1 renames P-identifiers, `docs/PREFERENCES.md:69` will contain a broken anchor (`#p-1-select-isolation`). After Stage 2 adds a Policies section, existing anchor links into author-adr SKILL.md (from `docs/PREFERENCES.md:52–56`) should remain valid since those sections aren't renamed — but this should be confirmed. **Recommended verification:** `grep -rn 'SKILL.md#' docs/ src/ --include='*.md' | grep -v '^docs/plans/' | grep -v '^docs/adr/'` — then verify each listed anchor resolves to an actual heading.

- **TG4.3** — **solve-adr regression check.** The dev plan says solve-adr is "already compliant — no changes needed" but Task 4.1 still checks it. The QA executor should confirm solve-adr SKILL.md was not accidentally modified and its existing Policies section (P-1 through P-4) remains intact.

---

## Recommendations

| # | Stage | Classification | Finding | Recommendation | Status |
|---|-------|----------------|---------|----------------|--------|
| 1 | 1 | Quality concern | TG1.1: `docs/PREFERENCES.md:69` anchor `#p-1-select-isolation` will break when P-1 is renamed to E-1 | Add acceptance criterion to Task 1.1: update `docs/PREFERENCES.md` anchor from `#p-1-select-isolation` to `#e-1-select-isolation` (or the correct anchor for the new heading) | ✅ Remediated — anchor updated to `#e-1-select-isolation` in commit 50b204d |
| 2 | 1 | Quality concern | TG1.2: `make check-refs` does not validate anchors — cannot catch TG1.1 | Add a manual grep verification step: `grep -rn 'prototype-adr/SKILL.md#p-' docs/ src/ --include='*.md'` must return 0 results after Stage 1 | ✅ Verified — grep returns 0 matches outside plan/adr docs |
| 3 | 1 | Low-severity | TG1.3: Identifier scheme consistency not mechanically verified | Add acceptance criterion: `grep -cP '### E-[0-6]:' src/skills/prototype-adr/SKILL.md` returns 7, confirming consistent prefix and count | ✅ Verified — returns 7 (E-0 through E-6, sequential, consistent prefix) |
| 4 | 2 | Quality concern | TG2.1: Semantic content preservation lacks mechanical verification | Add acceptance criterion: diff the original inline paragraph (line 118) against the new P-1 section — every sentence from the original must appear in the elevated version | ✅ Verified — all three key phrases confirmed at line 23: "always use the configured agent," "do not substitute `general-purpose` or skip dispatch," "applies in all modes, including autonomous workflows." P-2 content identical to original guardrail text. |
| 5 | 2 | Low-severity | TG2.2: Back-reference format inconsistency between P-1 and P-2 back-refs | Document the intentional difference (P-2 retains `**Guardrail:**` prefix from original); verify both back-references cite valid policy IDs | ✅ Verified — line 136 refs P-1 (exists at line 21), line 218 refs P-2 (exists at line 25). Format difference is intentional (P-2 retains original `**Guardrail:**` prefix). |
| 6 | 2 | Low-severity | TG2.3: Document structure integrity after insertion | After insertion, verify `## Procedure` table still has 7 entries (A-0 through A-6) and `## Assets` table is intact | ✅ Verified — `grep -cP '^\| A-[0-6]'` returns 7; Assets table entries (0023, 0024, 0031 decisions + templates/) all resolve to existing files. |
| 7 | 2 | Low-severity | TG2.4: Line count should be verified at stage boundary, not deferred to Stage 4 | Run `wc -l src/skills/author-adr/SKILL.md` after Stage 2 and confirm < 500 lines | ✅ Verified — 314 lines, well under 500-line limit. |
| 8 | 3 | Quality concern | TG3.1: Evaluation completeness is vacuously satisfiable | Require enumeration of all enforcement-language statements in implement-adr SKILL.md with per-statement disposition (step-specific / cross-cutting) | ✅ Verified — evaluation enumerates 13 statements with line numbers and dispositions. 3 minor enforcement-language lines (82, 282, 402) not explicitly listed but all are trivially step-specific or covered by existing P-1. Conclusion (no new policies) is correct. |
| 9 | 4 | Quality concern | TG4.1: `make check-refs` missing from Task 4.1 acceptance criteria | Add `make check-refs` to Task 4.1 criteria per AGENTS.md P-8 | ✅ Verified — `make check-refs` was added to Task 4.1 criteria. Passes for implement-adr, prototype-adr, solve-adr. author-adr has 16 pre-existing broken refs (not introduced by this plan). |
| 10 | 4 | Quality concern | TG4.2: Repo-wide anchor integrity not verified by any criterion | Add grep-based anchor verification across `docs/` and `src/` for all renamed identifiers | ✅ Verified — Task 4.1 includes `grep -rn 'SKILL.md#p-[0-6]' docs/ src/` criterion. Confirmed returns 0 matches (excluding plans/adr docs). |
| 11 | 4 | Low-severity | TG4.3: solve-adr no-regression confirmation | Verify `git diff src/skills/solve-adr/SKILL.md` shows no changes after all stages complete | ✅ Verified — `git diff src/skills/solve-adr/SKILL.md` returns empty. P-1 through P-4 intact at lines 23, 29, 33, 40. |
