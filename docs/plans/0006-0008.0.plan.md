# Implementation Plan: Participation Modes & Meta-ADR Behavioral Policies

**Source ADRs:**
- [ADR-0006: Implement-adr checks user participation level after planning](docs/adr/0006-implement-adr-checks-user-participation-level-after-planning.md)
- [ADR-0007: Weighted participation mode based on task cost](docs/adr/0007-weighted-participation-mode-based-on-task-cost.md)
- [ADR-0008: Meta-ADR directory for skill behavioral policies](docs/adr/0008-meta-adr-directory-for-skill-behavioral-policies.md)

**Generated:** 2026-04-02
**Location:** `docs/plans/0006-0008.0.plan.md`
**Revision:** 0

---

## Stage 1: Meta-ADR Directory Infrastructure

This stage establishes the `.meta/` directory convention that provides persistent
storage for behavioral policies. It must come first because the participation
framework (Stage 2) uses `.meta/` to store and retrieve the user's preference.

### Task 1.1: Add `init-meta` target to author-adr Makefile    [small]

**Description:** Add a Makefile target to `author-adr/Makefile` that creates the
`.meta/` subdirectory inside the ADR directory. The target reads the ADR directory
location from `.adr-dir` (falling back to `docs/adr` if `.adr-dir` does not exist)
and creates `<adr-dir>/.meta/`. This target should also be called as part of the
existing `init` target so that `.meta/` is bootstrapped automatically when a new
ADR directory is initialized.

**Implementation Notes:**

Add after the existing `init` target:

```makefile
init-meta: ## Create .meta/ directory for behavioral policies
	@adr_dir=$$(cat .adr-dir 2>/dev/null || echo "docs/adr"); \
	if [ -d "$$adr_dir" ]; then \
		mkdir -p "$$adr_dir/.meta"; \
		echo "Created $$adr_dir/.meta/ for behavioral policies."; \
	else \
		echo "ADR directory $$adr_dir does not exist. Run 'make init' first."; \
		exit 1; \
	fi
```

Update the `init` target to chain `init-meta` after initialization:

```makefile
init: check-format ## Initialize ADR directory (DIR=path)
	# ... existing init logic ...
	@$(MAKE) -f $(lastword $(MAKEFILE_LIST)) init-meta
```

Update `.PHONY` to include `init-meta`.

**Test & Acceptance Criteria:**
- [x] `make init-meta` creates `docs/adr/.meta/` when `docs/adr/` exists
- [x] `make init-meta` reads from `.adr-dir` when present
- [x] `make init-meta` prints error when ADR directory is missing
- [x] `make init DIR=docs/adr` also creates `.meta/` as a side effect
- [x] Existing `init` behavior is unchanged for ADR bootstrapping

**Dependencies:** None

**ADR Reference:** ADR-0008, Decision §3 (Bootstrapping)

---

### Task 1.2: Document `.meta` convention in author-adr SKILL.md    [small]

**Description:** Add documentation to `author-adr/SKILL.md` explaining the `.meta`
subdirectory as part of ADR directory initialization. This goes in the "Tooling"
section near the `init` target documentation, and in the "Format Detection" section
where the ADR directory is bootstrapped.

**Implementation Notes:**

In the Tooling section, after the `make init` example, add:

```markdown
#### Meta-ADR Directory

The `init` target also creates `<adr-dir>/.meta/` — a subdirectory for storing
behavioral policies consumed by companion skills (e.g., `implement-adr`). Meta-ADRs
use the same Nygard format as project ADRs but are numbered independently.

To create the `.meta` directory for an existing ADR setup:

```bash
make -f <skill-root>/Makefile init-meta
```

See [ADR-0008](docs/adr/0008-meta-adr-directory-for-skill-behavioral-policies.md)
for the full convention.
```

**Test & Acceptance Criteria:**
- [x] author-adr SKILL.md documents the `.meta` directory convention
- [x] The `init-meta` Makefile target is referenced
- [x] SKILL.md stays under 500 lines

**Dependencies:** Task 1.1

**ADR Reference:** ADR-0008, Decision §3 (Bootstrapping), §5 (SKILL.md awareness)

---

### Task 1.3: Add behavioral policy reading to implement-adr SKILL.md    [medium]

**Description:** Enhance `implement-adr/SKILL.md` Step 0 (Locate ADRs) to also
scan for a `.meta/` subdirectory inside the ADR directory. If `.meta/` exists and
contains ADRs with status `Accepted`, apply those behavioral policies for the
session. Add a new "Behavioral Policies" section explaining the convention, what
policies are supported, and the silent fallback to session context.

**Implementation Notes:**

In Step 0, after listing ADRs (item 3), add:

```markdown
4. **Check for behavioral policies:** Look for `<adr-dir>/.meta/` directory.
   - If present, read all `.md` files in `.meta/`.
   - Apply any meta-ADR with status `Accepted` as a behavioral policy for this
     session (e.g., participation mode preference).
   - Ignore meta-ADRs with status `Proposed`, `Deprecated`, or `Superseded`.
   - If `.meta/` does not exist, proceed silently — behavioral preferences
     will be established interactively and stored in session context only.
```

Add a new top-level section "Behavioral Policies" (after "Deep References"):

```markdown
## Behavioral Policies

The skill supports persistent behavioral preferences stored as meta-ADRs in
`<adr-dir>/.meta/`. These are read during Step 0 and applied for the session.

### Supported Policies

| Policy | Meta-ADR Title Pattern | Effect |
|--------|----------------------|--------|
| Participation mode | "Participation mode: *" | Sets the default participation level (Full control, Guided, Autonomous, Weighted) |

### Persistence Hierarchy

1. **`.meta/` directory** — if `<adr-dir>/.meta/` exists and contains an
   `Accepted` meta-ADR for the policy, use it.
2. **Session context** — if `.meta/` is absent, store the preference in
   session context (ephemeral, current session only).

The skill never prompts the user to create `.meta/`. If the user wants
persistent policies, they should use the `author-adr` skill to run
`make init-meta`.
```

**Test & Acceptance Criteria:**
- [x] Step 0 includes `.meta/` directory check
- [x] Only `Accepted` meta-ADRs are applied as policies
- [x] Silent fallback documented: no error or prompt when `.meta/` is absent
- [x] New "Behavioral Policies" section added to SKILL.md
- [x] Persistence hierarchy documented (`.meta/` → session context)
- [x] SKILL.md stays under 500 lines

**Dependencies:** None (SKILL.md changes are independent of Makefile changes)

**ADR Reference:** ADR-0008, Decision §1 (Store in `.meta/`), §2 (Convention), §4 (Fallback), §5 (SKILL.md awareness)

---

## Stage 2: Participation Level Framework

This stage adds the core participation prompt and mode behaviors. It depends on
Stage 1's behavioral policy reading to check for an existing participation
preference before prompting.

### Task 2.1: Add participation prompt and mode behaviors to implement-adr SKILL.md    [medium]

**Description:** Add a new Step 5 (renumbering the existing Step 5 "Review and
Iterate" to Step 6) in `implement-adr/SKILL.md` that asks the user their desired
participation level after the plan has been written and ADR statuses updated. Define
the four participation modes and their behaviors. If a participation preference
already exists in `.meta/` (loaded in Step 0), skip the prompt and apply it.

**Implementation Notes:**

Insert between current Step 4 and Step 5:

```markdown
### Step 5 — Participation Check

After updating ADR statuses, determine how the user wants to participate during
implementation.

1. **Check for existing preference:** If a behavioral policy for participation
   mode was loaded in Step 0 (from `.meta/`), apply it and inform the user:
   > Using participation mode: **Guided** (from .meta policy).
   > Say "change mode" at any time to switch.
   Skip to Step 6.

2. **If no preference exists**, prompt:
   > How much participation would you like during implementation?
   > 1. **Full control** — I'll review each stage and select what to start
   > 2. **Guided** — Summarize the plan, let me pick stages or request changes
   > 3. **Autonomous** — Execute the plan, check in at major milestones
   > 4. **Weighted** — Automatically adjust based on task complexity

3. **Apply the chosen mode** for the remainder of the session. See the
   behavior table below.

4. **Offer to persist** — if `.meta/` exists, ask the user whether to save
   this preference as a meta-ADR. If `.meta/` does not exist, store in
   session context only (no prompt about creating `.meta/`).

#### Participation Mode Behaviors

| Mode | Granularity | Behavior |
|------|-------------|----------|
| **Full control** | Per-stage | Present each stage individually. Wait for explicit approval before starting. After each stage, ask which to proceed with next. |
| **Guided** (default) | Plan-level | Summarize the full plan. Ask if changes are needed or which stages to start. Proceed with approved stages, reporting at stage boundaries. |
| **Autonomous** | Milestone-only | Execute all stages in order. Report at stage boundaries but do not wait for approval. Pause only on errors or ambiguity. |
| **Weighted** | Per-task, cost-driven | Evaluate each task by cost estimate. `[small]` → autonomous. `[medium]`/`[heavy]` → sentinel (pause for approval). Report at stage boundaries. |

**Mode switching:** The user may change modes at any time during a session.
In-session changes override the loaded preference but do not update `.meta/`.
```

Renumber the existing "Step 5 — Review and Iterate" to "Step 6 — Review and
Iterate". Update the agent workflow diagram at the top of SKILL.md to reflect
the new step.

**Test & Acceptance Criteria:**
- [x] New Step 5 added between existing Steps 4 and 5
- [x] Existing Step 5 renumbered to Step 6 with no content changes
- [x] All four modes documented in the behavior table
- [x] Skip-prompt logic documented when `.meta/` preference exists
- [x] Mode-switching documented
- [x] Guided is marked as the default
- [x] SKILL.md stays under 500 lines

**Dependencies:** Task 1.3 (behavioral policy reading must be in place)

**ADR Reference:** ADR-0006, Decision §1 (Participation check), §2 (Behavior by level); ADR-0007, Decision §1 (Add Weighted mode), §3 (Interaction with other modes)

---

### Task 2.2: Add behavioral decision capture logic to implement-adr SKILL.md    [medium]

**Description:** Extend the participation check (Step 5) to include logic for
persisting the user's choice. When `.meta/` exists, offer to write a meta-ADR.
When `.meta/` does not exist but `docs/adr/` does, fall back to session context
storage. Document the fallback chain and the convention for writing meta-ADRs.

Also document the general principle (ADR-0006, Decision §6): when the skill
encounters *any* behavioral decision during implementation that is not yet
recorded, it should attempt to capture it using the same fallback chain.

**Implementation Notes:**

Add to Step 5, after "Offer to persist" (item 4):

```markdown
#### Capturing Behavioral Decisions

When persisting a participation preference (or any behavioral decision):

1. **If `.meta/` exists** — write a meta-ADR in Nygard format:
   - Determine the next number by listing existing files in `.meta/`.
   - Title: "Participation mode: [chosen mode]"
   - Status: `Accepted`
   - Context: describe the user's reasoning if provided.
   - Decision: record the chosen mode and any customizations.
   - Consequences: note that future sessions will use this mode by default.

2. **If `.meta/` does not exist** — store in session context only.
   Do not prompt to create `.meta/`.

This same fallback chain applies whenever the skill encounters a behavioral
decision during implementation (e.g., error handling strategy, stage ordering
preference) that is not yet recorded.
```

**Test & Acceptance Criteria:**
- [x] Meta-ADR writing convention documented (Nygard format, naming, status)
- [x] Fallback chain documented: `.meta/` → session context
- [x] No prompt to create `.meta/` when it doesn't exist
- [x] General behavioral decision capture principle documented (ADR-0006 §6)
- [x] SKILL.md stays under 500 lines

**Dependencies:** Task 2.1

**ADR Reference:** ADR-0006, Decision §3 (Capture as ADR), §4 (Fallback), §6 (Other behavioral decisions); ADR-0008, Decision §1, §4

---

### Task 2.3: Document Weighted mode per-task evaluation behavior    [medium]

**Description:** Add detailed documentation for the Weighted participation mode's
per-task evaluation behavior in `implement-adr/SKILL.md`. This includes the
cost-driven rule (small → autonomous, medium/heavy → sentinel), stage boundary
reporting, and a worked example showing how Weighted mode processes a stage with
mixed-cost tasks.

**Implementation Notes:**

Add as a subsection under the participation mode behaviors in Step 5:

```markdown
#### Weighted Mode — Per-Task Evaluation

In Weighted mode, the skill evaluates each task independently as it proceeds
through a stage, using the task's cost estimate:

| Task Cost | Behavior | Agent Action |
|-----------|----------|-------------|
| `[small]` | Autonomous | Execute immediately, no approval needed |
| `[medium]` | Sentinel | Pause, summarize what's next, wait for approval |
| `[heavy]` | Sentinel | Pause, summarize what's next, wait for approval |

**Example:**

```
Stage 2: Authentication
  Task 2.1: Add auth config constants        [small]  → autonomous ✓
  Task 2.2: Create user model from schema    [small]  → autonomous ✓
  Task 2.3: Implement JWT middleware          [medium] → sentinel ⏸
  Task 2.4: Add rate limiting to auth routes  [small]  → autonomous ✓
  Task 2.5: Write integration tests           [medium] → sentinel ⏸
```

After the user approves a sentinel task and it completes, the skill continues
evaluating subsequent tasks by the same rule.

At the end of each stage, the skill reports a summary of what was completed,
regardless of whether individual tasks were autonomous or guided.
```

**Test & Acceptance Criteria:**
- [x] Per-task cost-driven rule documented in a table
- [x] Worked example with mixed-cost tasks included
- [x] Stage boundary reporting behavior documented
- [x] Sentinel pause/resume flow described
- [x] SKILL.md stays under 500 lines

**Dependencies:** Task 2.1

**ADR Reference:** ADR-0007, Decision §2 (Weighted mode behavior)

---

## Stage 3: Finalize

### Task 3.1: Update ADR statuses to Accepted    [small]

**Description:** Update the status of each source ADR from `Planned` to
`Accepted` to reflect that the decisions have been fully implemented.

**Files to update:**
- `docs/adr/0006-implement-adr-checks-user-participation-level-after-planning.md` — change `## Status` from `Planned` to `Accepted`
- `docs/adr/0007-weighted-participation-mode-based-on-task-cost.md` — change `## Status` from `Planned` to `Accepted`
- `docs/adr/0008-meta-adr-directory-for-skill-behavioral-policies.md` — change `## Status` from `Planned` to `Accepted`

**Test & Acceptance Criteria:**
- [x] ADR-0006 status reads `Accepted`
- [x] ADR-0007 status reads `Accepted`
- [x] ADR-0008 status reads `Accepted`
- [x] No other ADR content is modified
- [x] Supersession notes preserved (ADR-0006 partially superseded by 0007, ADR-0007 partially supersedes 0006)

**Dependencies:** All prior stages

**ADR Reference:** ADR-0003, Decision §2

---

## Summary

| Stage | Task | Cost | Dependencies |
|-------|------|------|--------------|
| 1. Meta-ADR Infrastructure | 1.1 Add `init-meta` to author-adr Makefile | small | — |
| 1. Meta-ADR Infrastructure | 1.2 Document `.meta` in author-adr SKILL.md | small | 1.1 |
| 1. Meta-ADR Infrastructure | 1.3 Add behavioral policy reading to implement-adr SKILL.md | medium | — |
| 2. Participation Framework | 2.1 Add participation prompt and mode behaviors | medium | 1.3 |
| 2. Participation Framework | 2.2 Add behavioral decision capture logic | medium | 2.1 |
| 2. Participation Framework | 2.3 Document Weighted per-task evaluation | medium | 2.1 |
| 3. Finalize | 3.1 Update ADR statuses to Accepted | small | All prior |

**Total estimated cost:** 3 small, 4 medium, 0 heavy
