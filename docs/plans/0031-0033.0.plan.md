# Implementation Plan: Add dispatch hooks, draft worksheet, and defer verb to author-adr

**Source ADRs:**
- [ADR-0031: Add author-adr dispatch hooks for custom agent delegation](docs/adr/0031-add-author-adr-dispatch-hooks-for-custom-agent-delegation.md)
- [ADR-0032: Add draft worksheet to author-adr workflow](docs/adr/0032-add-draft-worksheet-to-author-adr-workflow.md)
- [ADR-0033: Add defer verb to revise task triage](docs/adr/0033-add-defer-verb-to-revise-task-triage.md)

**Generated:** 2026-04-05
**Location:** `docs/plans/0031-0033.0.plan.md`
**Revision:** 0

---

## Task Execution Protocol

When executing any task from this plan, follow this protocol:

1. **Before starting a task** — read the task's **Test & Acceptance Criteria**
   section in full.
2. **As each criterion is satisfied** — immediately edit this plan file to mark
   the checkbox from `- [ ]` to `- [x]`.
3. **After completing all implementation work** for a task — review the criteria
   list one final time. Any criterion still unchecked is either incomplete work
   (go back and finish it) or an oversight (check it off with a brief inline
   note if it was already satisfied by prior work).
4. **Do not batch checkbox updates** to the end of a stage — update them
   incrementally so that progress is visible in the file at all times.

---

## Stage 1: Defer Verb (ADR-0033)

Add the Defer triage verb to the revise task, enabling authors to redirect
findings to other ADRs instead of the binary Address/Reject vocabulary.

### Task 1.1: Add Defer as third triage verb in revise.md    [small]

**Description:** Update `references/revise.md` Step 3 (Collect User Response)
to offer three choices instead of two: Address, Reject, and Defer. Add the
Defer mechanics: acknowledge the concern, state the scope boundary, and
provide a redirect destination. Update the step heading and body text.

**Implementation Notes:**

Replace the current Step 3 body with three choices:

```markdown
For each comment, offer the user three choices:

- **Address** — the user provides revised text, additional context, or agrees
  with the suggested wording. The agent notes the revision to apply.

- **Reject** — the user marks the comment as considered but declined. The user
  may optionally provide a reason. Record the rejection with its reason.

- **Defer** — the concern is valid but out of scope for this ADR. The user
  provides a redirect destination (e.g., "ADR-NNNN," "the implementing ADR,"
  "a future decision about X"). Record the deferral with its redirect.
```

Add a Defer mechanics subsection explaining the three-step process:
1. Acknowledge the concern
2. State the scope boundary
3. Provide redirect destination

**Test & Acceptance Criteria:**
- [x] Step 3 heading unchanged ("Collect User Response")
- [x] Step 3 body lists three choices: Address, Reject, Defer
- [x] Defer description includes redirect destination requirement
- [x] Defer mechanics (acknowledge, scope boundary, redirect) documented
- [x] Existing Address and Reject descriptions preserved unchanged
- [x] No modifications to Steps 1, 2, or 4

**Dependencies:** None

**ADR Reference:** ADR-0033, Decision §Verb Definitions, §Defer Mechanics

---

### Task 1.2: Update revision summary and Q&A addendum formats    [small]

**Description:** Update three sections in `references/revise.md`:

1. **Step 5 (Revision Summary)** — add `Deferred` as a third action value in
   the summary table. Update the summary line from "X addressed, Y rejected"
   to "X addressed, Y deferred, Z rejected."

2. **Step 5b (Q&A Addendum)** — add the `**Deferred**` prefix format with
   redirect arrow convention. Example:
   ```markdown
   ### Q: Should the ADR include a revisit trigger?

   **Deferred** — Not in scope for this ADR; revisit conditions will emerge
   when downstream ADRs define specific tools. → Follow-up: tooling ADR
   ```

3. **Output Format** — update the example summary table and format to include
   Deferred action. Update the Q&A format example to show all three prefixes.

**Test & Acceptance Criteria:**
- [ ] Step 5 summary table shows Deferred as a valid Action value
- [ ] Step 5 summary line uses three-way format: "X addressed, Y deferred, Z rejected"
- [ ] Step 5b includes `**Deferred**` prefix with `→` redirect convention
- [ ] Step 5b example shows a complete Deferred Q&A entry
- [ ] Output Format section updated with three-way summary example
- [ ] Output Format Q&A section shows all three prefixes (Addressed, Deferred, Rejected)
- [ ] Backwards compatible: existing two-verb (Address/Reject) ADR entries remain valid
- [ ] No unintended changes to other revise.md sections

**Dependencies:** Task 1.1

**ADR Reference:** ADR-0033, Decision §Q&A Addendum Format, §Revision Summary Format

---

### Task 1.3: Update SKILL.md revise workflow summary    [small]

**Description:** Update the "Revising an ADR" section in SKILL.md to reflect
that Step 3 now offers three choices (address, reject, or defer) instead of
two. This is the user-facing summary that references revise.md.

**Test & Acceptance Criteria:**
- [ ] SKILL.md Revising section Step 3 mentions three choices (address, reject, defer)
- [ ] No other SKILL.md sections modified
- [ ] Minor semantic change acknowledged (per ADR-0033 Quality Strategy)

**Dependencies:** Task 1.1

**ADR Reference:** ADR-0033, Decision §Verb Definitions

---

## Stage 2: Dispatch Hooks (ADR-0031)

Add configurable agent dispatch to the author-adr review→revise workflow
via `[author.dispatch]` TOML table with `review` and `editor` hooks.

### Task 2.1: Document [author.dispatch] config in SKILL.md    [medium]

**Description:** Add documentation for the `[author.dispatch]` table to the
SKILL.md Configuration section, after the existing `[author]` config docs.
Document:

1. The dispatch table schema: `review` (default: `"general-purpose"`) and
   `editor` (default: `"interactive"`) keys
2. The `"interactive"` reserved keyword meaning
3. The "same instructions, configurable executor" contract
4. Default behavior preservation (no dispatch table = current behavior)
5. Agent reference validation (graceful fallback on unresolved references)

Also add the dispatch table read to the Format Detection workflow step
(existing Step 1 reads `[author].template`; add a sub-step to read
`[author.dispatch]` keys).

**Implementation Notes:**

Add after the existing `### Project-Scoped Directory (.adr/)` section:

```markdown
### Agent Dispatch (`[author.dispatch]`)

Per ADR-0031, the review→revise workflow supports configurable agent dispatch.
Each hook point can be set to a specific agent or left at its default.

```toml
[author.dispatch]
review = "general-purpose"   # Agent for structured review (default)
editor = "interactive"       # Agent for editorial decisions (default: user)
```

| Hook | Default | Role | Instructions |
|------|---------|------|-------------|
| `review` | `"general-purpose"` | Reviewer | Receives `review.md` as prompt |
| `editor` | `"interactive"` | Editor | Receives `revise.md` as prompt |

The `"interactive"` value means "prompt the user directly." Any other value
is an agent reference. If a configured agent cannot be resolved, fall back
to the default and warn.
```

**Test & Acceptance Criteria:**
- [ ] `[author.dispatch]` table documented in SKILL.md Configuration section
- [ ] `review` and `editor` hook keys documented with defaults
- [ ] `"interactive"` reserved keyword explained
- [ ] "Same instructions, configurable executor" contract described
- [ ] Graceful fallback behavior documented (unresolved agent → default + warning)
- [ ] Format Detection step updated to read `[author.dispatch]` from config
- [ ] Default behavior preservation stated: no dispatch table = current behavior
- [ ] Version bump: major semantic change acknowledged (per ADR-0031 Quality Strategy)

**Dependencies:** None

**ADR Reference:** ADR-0031, Decision §Dispatch Table Schema, §Dispatch Contract, §Two Roles

---

### Task 2.2: Update review workflow for dispatch    [medium]

**Description:** Update the "Reviewing an ADR" section in SKILL.md and the
`references/review.md` file to support the `review` dispatch hook.

In SKILL.md:
- Update the review workflow description to note that the review agent is
  configurable via `[author.dispatch].review`
- Change "Use it as a prompt for a general-purpose agent" to reference the
  configured agent

In `references/review.md`:
- Update the introductory text to note it serves as the prompt for the
  configured reviewer agent (not just "general-purpose")
- Add a note that custom agents receive the same instructions; their persona
  shapes judgment, not task structure

**Test & Acceptance Criteria:**
- [ ] SKILL.md review section references configurable review agent
- [ ] review.md intro updated to reference configurable agent dispatch
- [ ] review.md notes that custom agents receive same instructions
- [ ] Default behavior unchanged when no dispatch config exists
- [ ] No modifications to review checklist content (Steps 1-6)

**Dependencies:** Task 2.1

**ADR Reference:** ADR-0031, Decision §Dispatch Contract, §Two Roles (review)

---

### Task 2.3: Update revise workflow for editor dispatch    [medium]

**Description:** Update the "Revising an ADR" section in SKILL.md and
`references/revise.md` to support the `editor` dispatch hook.

In SKILL.md:
- Update the revise workflow description to note that an editor agent can
  stand in for the user when `[author.dispatch].editor` is configured

In `references/revise.md`:
- Add a new section "Editor Dispatch" (after Guard Rails) explaining:
  - When `editor` is set to an agent reference (not `"interactive"`), the
    configured agent handles consequence validation and finding triage
  - The agent receives revise.md as its prompt with the review output
  - The agent's persona shapes its Address/Reject/Defer decisions
  - Step 6 (Re-review): when editor is delegated, the editor agent decides
    whether to re-review (not the user)
- Update Step 3 to note that "the user" may be the configured editor agent

**Test & Acceptance Criteria:**
- [ ] SKILL.md revise section mentions editor dispatch capability
- [ ] revise.md has new "Editor Dispatch" section after Guard Rails
- [ ] Editor dispatch section explains agent-as-user-stand-in behavior
- [ ] Step 3 acknowledges that "the user" may be the editor agent
- [ ] Step 6 (Re-review) notes editor agent decides when delegated
- [ ] Default behavior unchanged when editor = "interactive" or absent
- [ ] Guard rails preserved: one finding at a time, preserve author voice, etc.
- [ ] Editor persona Defer mapping documented: Defer implements the v3 principle 1 IMPORTANT clause ("rejection ≠ ignoring — always note where the concern DOES belong"). Note that persona `.agent.md` files are user-created custom agents outside this repo; the mapping is conveyed through revise.md instructions which the persona receives as its prompt.
- [ ] Multi-round convergence behavior noted: when editor is delegated and re-review triggers, the editor→review→revise loop runs per the dispatch config until verdict is Accept or cycle limit is reached

**Dependencies:** Task 2.1, Task 1.1 (Defer verb must be in place since editor uses it)

**ADR Reference:** ADR-0031, Decision §Two Roles (editor), §Dispatch Contract

---

## Stage 3: Draft Worksheet (ADR-0032)

Add a draft worksheet to the Comments area of the ADR, capturing original
intent and workflow calibration before the create/solve workflow runs.

### Task 3.1: Add draft mode to SKILL.md workflow    [medium]

**Description:** Update SKILL.md to add draft mode activation and the
"Drafting an ADR" workflow entry point.

1. Add a routing entry in the Agent Workflow dispatch table:
   ```
   ├─ "Draft an ADR / start a draft" ──► Go to: Drafting an ADR
   ```

2. Add a new "Drafting an ADR" section (before Creating an ADR) that:
   - Describes draft mode activation triggers ("draft an ADR", "start a
     draft", "I have an idea for a decision")
   - Creates the ADR file via `make new TITLE="tbd"`
   - Populates the Draft Worksheet in the Comments section
   - Describes the five worksheet dimensions (Framing, Tolerance, Uncertainty,
     Options, Candidates)
   - Shows the worksheet structure per ADR-0032's decision
   - Describes fill modes: pre-draft (create) and post-intake (solve)
   - After worksheet is filled, hands off to Creating or Solving workflow

**Test & Acceptance Criteria:**
- [ ] SKILL.md routing table includes draft mode entry
- [ ] New "Drafting an ADR" section added before "Creating an ADR"
- [ ] Draft mode creates ADR with `make new TITLE="tbd"`
- [ ] Worksheet structure matches ADR-0032 decision (5 dimensions)
- [ ] Both fill modes documented (pre-draft and post-intake)
- [ ] Handoff to create/solve workflow after worksheet completion
- [ ] Draft mode is optional — users can skip straight to create/solve
- [ ] Comments area role expansion documented (per ADR-0032 consequences)
- [ ] Major semantic change acknowledged (per ADR-0032 Quality Strategy)

**Dependencies:** None

**ADR Reference:** ADR-0032, Decision §Draft Worksheet Structure, §Workflow Integration, §Worksheet Fill Modes

---

### Task 3.2: Update create.md for worksheet reading    [small]

**Description:** Update `references/create.md` to read the Draft Worksheet
from the Comments section when present. Add worksheet consumption at the
start of Step 3 (Draft the ADR):

- If a Draft Worksheet exists in Comments, read it to:
  - Seed the Context section from the Framing field
  - Calibrate option depth based on tolerance levels
  - Pre-populate Options from the Candidates field
- If no worksheet exists, proceed as before (no change to current behavior)

**Test & Acceptance Criteria:**
- [ ] Step 3 checks for Draft Worksheet in Comments section
- [ ] Framing field seeds Context section
- [ ] Tolerance levels calibrate option depth
- [ ] Candidates pre-populate Options section
- [ ] Backwards compatible: no worksheet = current behavior unchanged
- [ ] No modifications to Steps 1, 2, 4, 5, or 6

**Dependencies:** Task 3.1

**ADR Reference:** ADR-0032, Decision §Workflow Integration (Create workflow), §Worksheet Fields

---

### Task 3.3: Update solve.md for worksheet integration    [small]

**Description:** Update `references/solve.md` to read the Draft Worksheet.
Add worksheet consumption at Step 1 (Problem Intake) and Step 2 (Option
Discovery):

Step 1:
- If a Draft Worksheet exists, use Framing as the initial problem statement
- Use Uncertainty to identify known vs. unknown areas

Step 2:
- Use Target count and "Explore additional" checkbox to scope option discovery
- Apply Improvisation tolerance to control divergence from the framing

**Test & Acceptance Criteria:**
- [ ] Step 1 reads Framing and Uncertainty from worksheet when present
- [ ] Step 2 reads Target count, Explore additional, and Improvisation tolerance
- [ ] Candidates from worksheet used as starting options in Step 2
- [ ] Backwards compatible: no worksheet = current behavior unchanged
- [ ] No modifications to Steps 3-6

**Dependencies:** Task 3.1

**ADR Reference:** ADR-0032, Decision §Workflow Integration (Solve workflow), §Worksheet Fields

---

### Task 3.4: Update revise.md for editor worksheet consumption    [medium]

**Description:** Update `references/revise.md` to instruct the editor (human
or persona agent) to read the Draft Worksheet for intent grounding during
triage. Add an "Intent Grounding" subsection to the Editor Dispatch section
(added in Task 2.3).

Key content:
- The editor reads the Draft Worksheet for intent grounding during
  Address/Reject/Defer decisions
- Tolerance levels are **exploration calibration signals**, NOT finding triage
  policy — this is the critical calibration from ADR-0032's prototype
- Framing helps assess whether a finding aligns with the author's direction
- Uncertainty helps distinguish "the author knew about this" from "this is a
  gap the author acknowledged"

Include an explicit calibration warning:
```
⚠️ Tolerance levels control how much the agent diverges during OPTION
DISCOVERY. They do NOT control how aggressively the editor accepts or rejects
review findings. Low improvisation tolerance means "stay close to the framing
when exploring options," not "reject findings that challenge the approach."
```

Also update the Q&A append logic to work when a Draft Worksheet precedes the
Q&A entries in Comments. The existing `<!-- Generated by the revise task -->`
detection logic should append AFTER the worksheet, not at the start of
Comments.

**Test & Acceptance Criteria:**
- [ ] "Intent Grounding" subsection added to Editor Dispatch section
- [ ] Tolerance calibration warning included (exploration ≠ triage policy)
- [ ] Framing and Uncertainty consumption described for triage decisions
- [ ] Q&A append logic verified to work with preceding Draft Worksheet
- [ ] Existing `<!-- Generated by the revise task -->` detection unchanged
- [ ] Backwards compatible: no worksheet = editor behavior unchanged

**Dependencies:** Task 2.3 (Editor Dispatch section must exist), Task 3.1

**ADR Reference:** ADR-0032, Decision §Workflow Integration (Editor integration), §Worksheet Fields, Quality Strategy §Editor tolerance calibration

---

## Stage 4: Documentation and Backwards Compatibility

### Task 4.1: Update SKILL.md deep references list    [small]

**Description:** Verify the Deep References section in SKILL.md accurately
reflects the current state of all reference files after the changes from
Stages 1-3. No new reference files are created — the existing files are
updated — so the references list should remain unchanged. Verify that all
`[references/*.md]` links resolve correctly.

**Test & Acceptance Criteria:**
- [ ] All reference links in SKILL.md Deep References resolve to existing files
- [ ] No broken internal links in SKILL.md
- [ ] `make -f src/skills/author-adr/Makefile check-refs` passes (if target exists) or manual verification
- [ ] `make validate` passes (skill frontmatter validation after SKILL.md changes)
- [ ] SKILL.md stays under 500 lines (spec recommendation)

**Dependencies:** All Stage 1-3 tasks

**ADR Reference:** ADR-0031, ADR-0032, ADR-0033 Quality Strategy §User documentation

---

### Task 4.2: Verify backwards compatibility    [small]

**Description:** Run the existing test suite to confirm that no changes
break existing behavior. The changes in Stages 1-3 are additive — they add
new capabilities without modifying existing script behavior.

Verify:
- `make test` passes (all 14 tests)
- Existing ADRs without dispatch config, worksheets, or Defer entries work
  identically to before

**Test & Acceptance Criteria:**
- [ ] `make test` passes — all 14 tests green
- [ ] No regressions in existing script behavior
- [ ] Existing ADRs (without new features) are unaffected

**Dependencies:** All Stage 1-3 tasks

**ADR Reference:** ADR-0031, ADR-0032, ADR-0033 Quality Strategy §Backwards Compatible

---

## Stage 5: Finalize

### Task 5.1: Update ADR statuses to Accepted    [small]

**Description:** Update the status of each source ADR from `Planned` to
`Accepted` to reflect that the decision has been fully implemented.

**Files to update:**
- `docs/adr/0031-add-author-adr-dispatch-hooks-for-custom-agent-delegation.md` — change `Status` from `Planned` to `Accepted`
- `docs/adr/0032-add-draft-worksheet-to-author-adr-workflow.md` — change `Status` from `Planned` to `Accepted`
- `docs/adr/0033-add-defer-verb-to-revise-task-triage.md` — change `Status` from `Planned` to `Accepted`

**Test & Acceptance Criteria:**
- [ ] ADR-0031 status reads `Accepted`
- [ ] ADR-0032 status reads `Accepted`
- [ ] ADR-0033 status reads `Accepted`
- [ ] No other ADR content is modified

**Dependencies:** All prior stages

**ADR Reference:** ADR-0003, Decision §2

---

## Summary

| Stage | Task | Cost | Dependencies |
|-------|------|------|--------------|
| 1. Defer Verb | 1.1 Add Defer verb to revise.md Step 3 | small | — |
| 1. Defer Verb | 1.2 Update summary and Q&A formats | small | 1.1 |
| 1. Defer Verb | 1.3 Update SKILL.md revise summary | small | 1.1 |
| 2. Dispatch Hooks | 2.1 Document [author.dispatch] config | medium | — |
| 2. Dispatch Hooks | 2.2 Update review workflow for dispatch | medium | 2.1 |
| 2. Dispatch Hooks | 2.3 Update revise workflow for editor dispatch | medium | 2.1, 1.1 |
| 3. Draft Worksheet | 3.1 Add draft mode to SKILL.md | medium | — |
| 3. Draft Worksheet | 3.2 Update create.md for worksheet | small | 3.1 |
| 3. Draft Worksheet | 3.3 Update solve.md for worksheet | small | 3.1 |
| 3. Draft Worksheet | 3.4 Update revise.md for editor worksheet | medium | 2.3, 3.1 |
| 4. Documentation | 4.1 Update deep references | small | Stages 1-3 |
| 4. Documentation | 4.2 Verify backwards compatibility | small | Stages 1-3 |
| 5. Finalize | 5.1 Update ADR statuses to Accepted | small | All prior |

**Total estimated cost:** 6 small, 5 medium, 0 heavy
