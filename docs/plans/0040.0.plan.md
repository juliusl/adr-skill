# Implementation Plan: Extract adr-db-lib crate for shared database access

**Source ADRs:**
- [ADR-0040: Extract adr-db-lib crate for shared database access](docs/adr/0040-extract-adr-db-lib-crate-for-shared-database-access.md)

**Generated:** 2026-04-06
**Location:** `docs/plans/0040.0.plan.md`
**Revision:** 0

---

## Task Execution Protocol

When executing any task from this plan, follow this protocol:

1. **Before starting a task** — read the task's **Test & Acceptance Criteria**
   section in full.
2. **As each criterion is satisfied** — immediately edit this plan file to mark
   the checkbox from `- [ ]` to `- [x]`.
3. **After completing all implementation work** for a task — review the criteria
   list one final time. Any criterion still unchecked is either incomplete work
   (go back and finish it) or an oversight (check it off with a brief inline
   note if it was already satisfied by prior work).
4. **Do not batch checkbox updates** to the end of a stage — update them
   incrementally so that progress is visible in the file at all times.

---

## Stage 1: Create adr-db-lib Crate

### Task 1.1: Scaffold adr-db-lib crate and move shared files    [medium]

**Description:**
Create the `crates/adr-db-lib/` directory structure. Move the following files from `adr-db` to `adr-db-lib`:
- `schema.rs` → `adr-db-lib/src/schema.rs` (unchanged)
- `models.rs` → `adr-db-lib/src/models.rs` (unchanged)
- `migrations/` → `adr-db-lib/migrations/` (entire directory)
- `diesel.toml` → `adr-db-lib/diesel.toml` (unchanged)

Create new files:
- `adr-db-lib/Cargo.toml` — dependencies: `diesel` (with sqlite feature), `diesel_migrations`, `serde` (with derive feature). No clap, no serde_json.
- `adr-db-lib/src/lib.rs` — `pub mod schema; pub mod models; pub mod db;`
- `adr-db-lib/src/db.rs` — connection helper (`establish_connection`) and embedded migrations (`MIGRATIONS`, `run_init`)

The `db.rs` module absorbs the logic currently in `adr-db/src/init.rs`: directory creation, connection establishment, and migration running. The `run_init` function signature stays the same.

Update `crates/Cargo.toml` workspace to include the new member:
```toml
[workspace]
members = ["adr-db", "adr-db-lib"]
resolver = "2"
```

**Test & Acceptance Criteria:**
- [x] `crates/adr-db-lib/` directory exists with `Cargo.toml`, `diesel.toml`, `src/`, `migrations/`
- [x] `lib.rs` exports `pub mod schema`, `pub mod models`, `pub mod db`
- [x] `db.rs` contains `establish_connection`, `run_init`, and `MIGRATIONS`
- [x] `schema.rs` and `models.rs` are identical to the originals
- [x] `migrations/` directory moved with both migration subdirectories intact
- [x] `adr-db-lib/Cargo.toml` does not depend on `clap` or `serde_json`
- [x] Workspace `Cargo.toml` lists both members
- [x] `cargo build -p adr-db-lib` succeeds
- [x] `cargo test -p adr-db-lib` succeeds

**Dependencies:** None

**ADR Reference:** ADR-0040, Decision §Library crate structure

---

## Stage 2: Rewire adr-db Binary Crate

### Task 2.1: Update adr-db to depend on adr-db-lib    [medium]

**Description:**
Refactor `adr-db` to consume shared code from `adr-db-lib` instead of owning it:

1. **Cargo.toml** — add `adr-db-lib = { path = "../adr-db-lib" }`. Remove `diesel` and `diesel_migrations` direct dependencies (they come transitively through `adr-db-lib`). Keep `clap` and `serde_json`.
2. **Remove** `schema.rs`, `models.rs`, `init.rs`, `diesel.toml`, and `migrations/` from `adr-db` — these now live in `adr-db-lib`.
3. **Update imports** in `main.rs`, `ingest.rs`, and `view.rs`:
   - `use crate::schema::*` → `use adr_db_lib::schema::*`
   - `use crate::models::*` → `use adr_db_lib::models::*`
   - `init::run_init` → `adr_db_lib::db::run_init`
   - Remove `mod init; mod models; mod schema;` from `main.rs`
4. **Update view.rs tests** — the `embed_migrations!()` calls in test helpers need to reference `adr_db_lib::db::MIGRATIONS` instead of embedding migrations locally.
5. **Update main.rs** — remove `mod init`, `mod models`, `mod schema`. Keep `mod ingest; mod view;`.

**Test & Acceptance Criteria:**
- [ ] `adr-db/Cargo.toml` depends on `adr-db-lib` via path
- [ ] `adr-db/Cargo.toml` does not list `diesel` or `diesel_migrations` directly
- [ ] `schema.rs`, `models.rs`, `init.rs`, `diesel.toml`, `migrations/` removed from `adr-db/`
- [ ] `main.rs` no longer declares `mod init`, `mod models`, `mod schema`
- [ ] All imports in `ingest.rs` and `view.rs` reference `adr_db_lib::`
- [ ] `cargo build -p adr-db` succeeds
- [ ] `cargo test --workspace` passes (all tests across both crates)
- [ ] `make build-tools` succeeds

**Dependencies:** Task 1.1

**ADR Reference:** ADR-0040, Decision §Binary crate changes

---

## Stage 3: Finalize

### Task 3.1: Update ADR status to Accepted    [small]

**Description:** Update the status of ADR-0040 from `Planned` to
`Accepted` to reflect that the decision has been fully implemented.

**Files to update:**
- `docs/adr/0040-extract-adr-db-lib-crate-for-shared-database-access.md` — change status from `Planned` to `Accepted`

**Test & Acceptance Criteria:**
- [ ] ADR-0040 status reads `Accepted`
- [ ] No other ADR content is modified

**Dependencies:** All prior stages

**ADR Reference:** ADR-0003, Decision §2

---

## Summary

| Stage | Task | Cost | Dependencies |
|-------|------|------|--------------|
| 1. Create adr-db-lib | 1.1 Scaffold crate and move shared files | medium | — |
| 2. Rewire adr-db | 2.1 Update adr-db to depend on adr-db-lib | medium | 1.1 |
| 3. Finalize | 3.1 Update ADR status to Accepted | small | All prior |

**Total estimated cost:** 1 small, 2 medium, 0 heavy
