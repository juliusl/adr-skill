# QA Plan: Run TPM Dispatch Parallel with UX/DX Review

**Source Plan:** docs/plans/0066.0.plan.md
**Generated:** 2026-04-10
**Location:** `docs/plans/0066.0.qa-plan.md`

---

## Stage 1: Parallel Dispatch Implementation

This stage modifies three documentation files (create.md, SKILL.md, ADR-0064 asset). No executable code is changed. Security and UX checklists are evaluated against the documentation change surface — items that cannot apply to documentation-only changes are marked N/A with justification.

### Security Checks

- [x] **S1.1** — N/A: No SQL, shell commands, or file paths are introduced — changes are to agent procedure prose
- [x] **S1.2** — No secrets, credentials, or API keys in committed files
- [x] **S1.3** — N/A: No deserialization paths — changes are markdown documentation
- [x] **S1.4** — N/A: No dependencies added or modified
- [x] **S1.5** — N/A: No new files created — only edits to existing markdown
- [x] **S1.6** — N/A: No new external input surfaces — dispatch hooks already exist

### UX Checks

- [x] **U1.1** — N/A: No error paths — documentation-only change
- [x] **U1.2** — N/A: No commands — documentation-only change
- [x] **U1.3** — N/A: No input validation — documentation-only change
- [x] **U1.4** — N/A: No resource handles — documentation-only change
- [x] **U1.5** — Procedure text is verifiable: `make check-refs` passes (all 4 skills valid); `make test` passes (32 tests)
- [x] **U1.6** — State change is inspectable: grep for stale sequential terms (`after Step 4a`, `consumes 4a`, `4a's findings`, etc.) returns zero matches
- [x] **U1.7** — All three files read and verified: SKILL.md dispatch table, create.md flow+conditional note, ADR-0064 asset annotation — parallel semantics consistent

### Test-Gap Findings

**TG1.1 — Exhaustive stale-language sweep**
Dev acceptance criteria for Tasks 1.1, 1.2, and 1.3 check specific locations (line 95, line 118, line 258, dispatch table). They do not require a repo-wide grep for residual sequential language (e.g., "after Step 4a", "runs after 4a", "consuming Step 4a's findings", "sequential"). A targeted phrase could survive in a location not enumerated in the plan.

**TG1.2 — Cross-reference consistency between SKILL.md and create.md**
Task 1.1 modifies create.md. Task 1.2 modifies SKILL.md. Neither task's acceptance criteria require verifying that the two files agree on dispatch semantics. If one says "parallel" and the other retains "sequential" language, all per-task criteria pass but the skill instructions are contradictory.

**TG1.3 — Verdict consolidation completeness**
Task 1.1 requires verdict consolidation logic with the priority order "Redesign > Revise — Major > Revise > Accept." The acceptance criterion checks that the logic is "defined" but does not verify:
- What happens when only Step 4a runs (no TPM configured)?
- What happens when only Step 4b runs (no UX/DX configured)?
- Whether the consolidation section explicitly handles partial dispatch (one-of-two agents runs).

**TG1.4 — ADR-0064 asset annotation placement**
Task 1.3 specifies an HTML comment annotation but does not verify that the annotation is placed at each location where sequential language appears in the asset — line 41 ("dispatch `tpm` with the ADR content and UX/DX findings") and line 119 ("UX/DX findings are an optional enrichment"). If only one location is annotated, the other remains contradictory.

---

## Stage 2: Resolve TODOS.md Items

### Security Checks

- [x] **S2.1** — N/A: No SQL, shell, or path interpolation — strikethrough formatting only
- [x] **S2.2** — No secrets in TODOS.md edits
- [x] **S2.3** — N/A: No deserialization
- [x] **S2.4** — N/A: No dependencies
- [x] **S2.5** — N/A: No new files
- [x] **S2.6** — N/A: No new input surfaces

### UX Checks

- [x] **U2.1** — N/A: No error paths
- [x] **U2.2** — N/A: No commands
- [x] **U2.3** — N/A: No input validation
- [x] **U2.4** — N/A: No resources
- [x] **U2.5** — TODOS.md edits verified: items #12 and #13 readable with strikethrough on all cells
- [x] **U2.6** — Strikethrough renders correctly (`~~...~~` on every cell in both rows)
- [x] **U2.7** — Items #12 and #13 struck through; triage reads "Done — resolved by ADR-0066"

### Test-Gap Findings

**TG2.1 — Resolution accuracy**
Dev criteria check formatting (strikethrough) and triage text, but do not verify that items #12 and #13 are actually resolved by the parallel dispatch change. If the implementation introduces a new form of the same ambiguity (e.g., consolidation order replaces execution order as the ambiguity), the items should not be closed. QA should verify the original concern is genuinely eliminated, not just reformulated.

---

## Stage 3: Validate

### Security Checks

- [x] **S3.1** — N/A: Validation stage runs existing `make` targets — no new code
- [x] **S3.2** — N/A: No new files
- [x] **S3.3** — N/A: No deserialization
- [x] **S3.4** — N/A: No dependencies
- [x] **S3.5** — N/A: No new artifacts
- [x] **S3.6** — N/A: No new input surfaces

### UX Checks

- [x] **U3.1** — N/A: Uses existing validated commands
- [x] **U3.2** — `make test` exit code 0; `make check-refs` exit code 0
- [x] **U3.3** — N/A: No new input paths
- [x] **U3.4** — N/A: No new resources
- [x] **U3.5** — 32 tests pass; all 4 skills report zero broken references
- [x] **U3.6** — N/A: No new state
- [x] **U3.7** — Test output is human-readable in terminal

### Test-Gap Findings

No test-gap findings for this stage. The validation stage is self-checking — it runs the existing mechanical tests.

---

## Stage 4: Finalize

### Security Checks

- [ ] **S4.1** — N/A: Single-word status change in ADR file — **NOT YET EXECUTED** (Stage 4 pending)
- [ ] **S4.2** — No secrets in status change — **NOT YET EXECUTED**
- [ ] **S4.3** — N/A: No deserialization
- [ ] **S4.4** — N/A: No dependencies
- [ ] **S4.5** — N/A: No new files
- [ ] **S4.6** — N/A: No new input surfaces

### UX Checks

- [ ] **U4.1** — N/A: No error paths — **NOT YET EXECUTED**
- [ ] **U4.2** — N/A: No commands
- [ ] **U4.3** — N/A: No input
- [ ] **U4.4** — N/A: No resources
- [ ] **U4.5** — ADR status change is verifiable by reading the file — **NOT YET EXECUTED**
- [ ] **U4.6** — Status field in ADR frontmatter is inspectable — **NOT YET EXECUTED**
- [ ] **U4.7** — A reviewer can verify the status reads "Accepted" — **NOT YET EXECUTED**

### Test-Gap Findings

**TG4.1 — ADR-0066 status value**
The source ADR currently shows `Status: Planned`. The plan says to change from `Proposed` to `Accepted`. QA should verify the actual current status and ensure the correct transition is applied.

---

## Recommendations

| # | Stage | Classification | Finding | Recommendation | Plan Ref | Status |
|---|-------|----------------|---------|----------------|----------|--------|
| 1 | 1 | Quality concern | **Stale sequential language may survive in unenumerated locations.** Dev criteria target specific lines but do not require a repo-wide sweep. | After implementation, run `grep -rn 'after Step 4a\|runs after 4a\|consuming Step 4a\|consuming.*4a.*findings\|4b.*after.*4a\|sequential.*4a.*4b' src/skills/author-adr/` and verify zero matches. | Task 1.1 criterion 1, Task 1.2 criterion 2 | ✅ Pass — grep returns zero stale matches; only parallel-affirmative language found |
| 2 | 1 | Quality concern | **Cross-reference consistency not verified.** No criterion requires checking that SKILL.md dispatch table, create.md flow diagram, create.md conditional note, and ADR-0064 asset all describe the same parallel semantics. | After all Stage 1 tasks, verify: (a) SKILL.md `tpm` row says "parallel with 4a", (b) create.md flow shows parallel notation, (c) create.md conditional note says "dispatch in parallel", (d) ADR-0064 asset has annotation. All four must agree. | Task 1.1, Task 1.2, Task 1.3 | ✅ Pass — all four locations confirmed consistent: SKILL.md:153 "parallel with 4a", create.md:112 parallel flow, create.md:119 parallel note, ADR-0064:43-44 annotation |
| 3 | 1 | Quality concern | **Verdict consolidation must handle partial dispatch.** When only 4a or only 4b runs, the consolidation section must state that the single agent's verdict applies directly — no merging needed. If this case is omitted, an executor agent may stall looking for a second verdict that doesn't exist. | Verify the new "Step 4 Verdict Consolidation" section explicitly handles: (1) both run, (2) only 4a runs, (3) only 4b runs, (4) neither runs. | Task 1.1 criterion 4 | ✅ Pass — (1) both run: priority table at lines 280-285; (2)+(3) single run: line 287 "When only one step ran…use that step's verdict directly"; (4) neither runs: implicitly covered — Step 4 baseline assessment (lines 205-221) applies when no sub-steps dispatch. Low-severity note: "neither runs" case could be stated explicitly for clarity. |
| 4 | 1 | Low-severity | **ADR-0064 asset has multiple sequential-language locations.** Line 41 references "UX/DX findings" as TPM input; line 119 calls findings "optional enrichment." Both should be annotated or updated for consistency. | Verify annotation covers both line 41 and line 119 of the ADR-0064 asset, or that the prose at those lines is updated to reflect parallel dispatch. | Task 1.3 criterion 2 | ⚠️ Partial — Annotation at lines 43-44 covers line 41 ✅. Line 122 still reads "UX/DX findings are an optional enrichment" with no annotation. Under ADR-0066, findings are not passed to TPM at all. Low-severity: the asset is a historical decision record and the annotation covers the primary location. |
| 5 | 2 | Low-severity | **TODOS resolution accuracy.** Strikethrough formatting does not prove the underlying concern is resolved — only that the item was marked done. | Verify item #12 (execution order) is genuinely eliminated by parallel dispatch (no ordering exists). Verify item #13 (Redesign not gating 4b) is genuinely eliminated (independent dispatch means no gating relationship). | Task 2.1 | ✅ Pass — #12: parallel dispatch eliminates sequential dependency entirely (no ordering exists); #13: independent parallel dispatch means neither step gates the other. Both concerns genuinely resolved. |
| 6 | 4 | Low-severity | **ADR-0066 current status mismatch.** The ADR file shows `Status: Planned` but the plan references changing from `Proposed`. | Verify the correct status transition: `Planned` → `Accepted`. Confirm the plan's reference to "Proposed" is not a blocker — the file content determines the transition. | Task 4.1 criterion 1 | ⚠️ Not yet executed — Stage 4 (finalize) has not been completed. ADR-0066 status is still `Planned`. Plan Task 4.1 criteria remain unchecked. Correct transition is `Planned → Accepted`. |
