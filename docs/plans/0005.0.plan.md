# Implementation Plan: Store implementation plans in docs/plans with versioned naming

**Source ADRs:**
- [ADR-0005: Store implementation plans in docs/plans with versioned naming](docs/adr/0005-store-implementation-plans-in-docs-plans-with-versioned-naming.md)

**Generated:** 2026-04-02

---

## Stage 1: Directory and Template Foundation

### Task 1.1: Create docs/plans/ directory with README    [small]

**Description:** Create the `docs/plans/` directory in the repository and add a
README.md explaining the naming convention and revision workflow. The README
serves as discoverable documentation for engineers browsing the directory.

**Implementation Notes:**

```markdown
# Implementation Plans

Plans generated by the `implement-adr` skill are stored here.

## Naming Convention

`<adr-range>.<revision>.plan.md`

- **adr-range:** Zero-padded ADR number(s) — `0003`, `0003-0004`, `0003-0004-0007`
- **revision:** Zero-based integer — `0` (initial), `1` (first revision), etc.

## Examples

- `0003-0004.0.plan.md` — initial plan for ADRs 3–4
- `0003-0004.1.plan.md` — first revision after user feedback
- `0007.0.plan.md` — initial plan for ADR 7 alone
```

**Test & Acceptance Criteria:**
- [ ] `docs/plans/` directory exists
- [ ] `docs/plans/README.md` documents the naming convention and revision workflow
- [ ] Directory is not gitignored

**Dependencies:** None

**ADR Reference:** ADR-0005, Decision §1 (Plan directory)

---

### Task 1.2: Update plan template with versioned header fields    [small]

**Description:** Update the plan template at
`implement-adr/assets/templates/plan-template.md` to include the new header
fields for revision tracking. Add `**Revision:**` and `**Location:**` fields
to the template header.

**Implementation Notes:**

Add these fields to the template header after `**Generated:**`:

```markdown
**Location:** `docs/plans/<adr-range>.<revision>.plan.md`

**Revision:** 0
```

For revisions > 0, the template should show:

```markdown
**Revision:** N (previous: [<adr-range>.<N-1>.plan.md](docs/plans/<adr-range>.<N-1>.plan.md))
**Changes:** <summary of requested changes>
```

**Test & Acceptance Criteria:**
- [ ] Template includes `**Location:**` field with the naming pattern
- [ ] Template includes `**Revision:**` field
- [ ] Template includes commented example of revision > 0 header

**Dependencies:** None

**ADR Reference:** ADR-0005, Decision §2 (Naming convention), §3 (Revision workflow)

---

## Stage 2: Skill Workflow Updates

### Task 2.1: Update SKILL.md Step 3 — plan output path    [small]

**Description:** Update the plan generation instructions in SKILL.md Step 3
(Generate the Implementation Plan) section 3a (Header and ADR References) to
use `docs/plans/` and the versioned naming convention instead of a plain
`plan.md`. The header example should show the `Location` and `Revision` fields.

**Implementation Notes:**

In section 3a, update the example header to:

```markdown
# Implementation Plan: [Title from ADR Decision]

**Source ADRs:**
- [ADR-0002: Add implement-adr companion skill](docs/adr/0002-add-implement-adr-companion-skill.md)

**Generated:** YYYY-MM-DD
**Location:** `docs/plans/0002.0.plan.md`
**Revision:** 0
```

**Test & Acceptance Criteria:**
- [ ] SKILL.md section 3a example header includes `Location` and `Revision`
- [ ] The naming convention `<adr-range>.<revision>.plan.md` is shown
- [ ] No other Step 3 content is changed

**Dependencies:** Task 1.2

**ADR Reference:** ADR-0005, Decision §2 (Naming convention)

---

### Task 2.2: Update SKILL.md Step 5 — write path and revision workflow    [medium]

**Description:** Update Step 5 (Review and Iterate) in SKILL.md to:
1. Write the plan to `docs/plans/<adr-range>.0.plan.md` instead of `plan.md` in
   the repository root.
2. Create `docs/plans/` if it does not exist.
3. When the user requests changes, increment the revision number and create a
   new file preserving the previous revision unchanged.

**Implementation Notes:**

Replace the current Step 5 content with:

```markdown
### Step 5 — Review and Iterate

1. Present the generated plan to the user.
2. Ask if any stages or tasks need adjustment.
3. If the user identifies additional gaps, go back to Step 2.
4. Once the user approves, write the plan to `docs/plans/` using the versioned
   naming convention:
   - Create `docs/plans/` if it does not exist.
   - File name: `<adr-range>.0.plan.md` (initial plan).
   - Example: `docs/plans/0003-0004.0.plan.md`
5. If the user requests changes to an existing plan:
   a. Increment the revision number.
   b. Create a new file (e.g., `0003-0004.1.plan.md`).
   c. Add a revision header linking to the previous revision:
      **Revision:** 1 (previous: [0003-0004.0.plan.md](docs/plans/0003-0004.0.plan.md))
      **Changes:** <summary of requested changes>
   d. Preserve the previous revision file unchanged.
```

**Test & Acceptance Criteria:**
- [ ] Step 5 references `docs/plans/` as the output directory
- [ ] Step 5 documents the revision workflow (increment, new file, link to previous)
- [ ] Step 5 instructs to create `docs/plans/` if missing
- [ ] No reference to writing `plan.md` in the repository root remains in SKILL.md

**Dependencies:** Task 2.1

**ADR Reference:** ADR-0005, Decision §3 (Revision workflow), §4 (SKILL.md updates)

---

### Task 2.3: Update Makefile list-adrs and add list-plans target    [small]

**Description:** Add a `list-plans` target to the implement-adr Makefile that
lists existing plans in `docs/plans/`. This mirrors the existing `list-adrs`
target and helps users discover plans.

**Implementation Notes:**

```makefile
list-plans: ## List implementation plans
	@if [ -d docs/plans ]; then \
		ls docs/plans/*.plan.md 2>/dev/null || echo "No plans found in docs/plans/"; \
	else \
		echo "No docs/plans/ directory found."; \
	fi
```

**Test & Acceptance Criteria:**
- [ ] `make list-plans` lists `.plan.md` files when `docs/plans/` exists
- [ ] `make list-plans` prints a helpful message when directory is missing
- [ ] Existing `list-adrs` target is unchanged

**Dependencies:** Task 1.1

**ADR Reference:** ADR-0005, Decision §4 (SKILL.md and template updates)

---

## Stage 3: Finalize

### Task 3.1: Update ADR-0005 status to Accepted    [small]

**Description:** Update the status of ADR-0005 from `Proposed` to `Accepted`
to reflect that the decision has been fully implemented.

**Files to update:**
- `docs/adr/0005-store-implementation-plans-in-docs-plans-with-versioned-naming.md` — change `## Status` from `Proposed` to `Accepted`

**Test & Acceptance Criteria:**
- [ ] ADR-0005 status reads `Accepted`
- [ ] No other ADR content is modified

**Dependencies:** All prior stages

**ADR Reference:** ADR-0003, Decision §2

---

## Summary

| Stage | Task | Cost | Dependencies |
|-------|------|------|--------------|
| 1. Foundation | 1.1 Create docs/plans/ with README | small | — |
| 1. Foundation | 1.2 Update plan template header | small | — |
| 2. Skill Workflow | 2.1 Update SKILL.md Step 3 output path | small | 1.2 |
| 2. Skill Workflow | 2.2 Update SKILL.md Step 5 revision workflow | medium | 2.1 |
| 2. Skill Workflow | 2.3 Add list-plans Makefile target | small | 1.1 |
| 3. Finalize | 3.1 Update ADR-0005 status to Accepted | small | All prior |

**Total estimated cost:** 5 small, 1 medium
