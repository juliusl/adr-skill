[package]
name = "adr-skills"
version = "0.1.0"
edition = "2021"
description = "CLI for installing ADR skills and agents"

[dependencies]
clap = { version = "4", features = ["derive"] }
rust-embed = "8"
dirs = "6"

[dev-dependencies]
tempfile = "3"
